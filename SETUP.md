# คู่มือติดตั้งระบบแจ้งเตือนปฏิทินงานเข้า LINE (Apps Script + Google Sheet)

ทำตามลำดับนี้ทีละขั้น ใช้เวลารวมประมาณ 20-30 นาที

## 1. เตรียม Google Sheet จาก template

ทางเลือก A — ใช้ template (สะดวกสุดสำหรับติดตั้งใหม่):

1. เปิด Google Drive > New > File upload > อัปโหลด `notify-system-template.xlsx`
2. คลิกขวาไฟล์ที่อัปโหลด > Open with > Google Sheets (หรือเปิดไฟล์แล้วเลือก File > Save as Google Sheets)
3. จะได้ Google Sheet ที่มี 3 แท็บ: `Settings`, `Holidays`, `Logs`

ทางเลือก B — สเปรดชีตเปล่า: ข้ามขั้นตอนนี้ไปทำข้อ 2 ตามปกติ แล้วรันเมนู **เตรียม/ตรวจสอบชีตทั้งหมด** (`setupSheet`) ครั้งแรก — ระบบจะสร้างชีตทั้ง 4 พร้อมหัวตาราง ค่าตั้งต้น และรูปแบบวันที่ให้เอง (template จึงเป็นแค่ทางลัด ไม่ใช่ข้อบังคับ)

## 2. เปิด Apps Script (ผูกกับชีตนี้โดยตรง)

1. ในชีตที่เพิ่งสร้าง ไปที่เมนู **Extensions > Apps Script**
2. จะได้โปรเจกต์ Apps Script ใหม่ที่ผูกกับสเปรดชีตนี้อัตโนมัติ (โค้ดจึงเรียก `SpreadsheetApp.getActive()` ได้เลย ไม่ต้องตั้ง Spreadsheet ID เอง)
3. ลบไฟล์ `Code.gs` เปล่าที่มีอยู่เดิม แล้วสร้างไฟล์ใหม่ 2 ไฟล์ชื่อ `Code.gs` และ `Webhook.gs` วางโค้ดจากไฟล์ที่แนบมาให้ลงไป

## 3. ตั้งค่า Secret ใน Script Properties

อย่าใส่ Token ในชีต — ไปที่ **Project Settings** (ไอคอนเฟือง) > **Script Properties** > **Add script property**

| Property | Value |
|---|---|
| `LINE_CHANNEL_ACCESS_TOKEN` | Channel access token จาก LINE Developers Console |
| `NOTION_TOKEN` | Internal Integration Secret จาก Notion (ดูวิธีสร้างในข้อ 4) |

(ยังไม่มี LINE channel? สร้างที่ [LINE Developers Console](https://developers.line.biz/console/) > สร้าง Provider > สร้าง Messaging API channel)

## 4. สร้าง Notion Integration แล้วเชื่อมกับ database

1. ไปที่ [notion.so/my-integrations](https://www.notion.so/my-integrations) > **New integration**
2. ตั้งชื่อ (เช่น "ระบบแจ้งเตือนปฏิทิน") เลือก workspace ที่มี database "ปฏิทินการปฏิบัติงาน" อยู่ > **Submit**
3. คัดลอก **Internal Integration Secret** ไปใส่ `NOTION_TOKEN` ตามตารางข้อ 3 ด้านบน
4. เปิดหน้า database "ปฏิทินการปฏิบัติงาน" ใน Notion จริง กดปุ่ม **"..."** มุมขวาบน > **Connections** > เพิ่ม integration ที่เพิ่งสร้าง — **ขั้นตอนนี้จำเป็น ไม่ใช่ทางเลือก** ต่อให้ token ถูกต้อง แต่ถ้าไม่แชร์ database ให้ integration ตรงนี้ API จะตอบ "object not found" เสมอ
5. คัดลอก **Database ID** จาก URL ของหน้า database (ส่วนของรหัสยาวๆ ก่อนเครื่องหมาย `?` ถ้ามี) ไปวางในชีต Settings แถว `notion_database_id`

โค้ดอ้างอิงชื่อ property ของ database ตรงๆ (งาน, วันที่ - เวลา, สถานะงาน, ผู้รับผิดชอบ, สถานที่, รายละเอียด, หมายเหตุ) — ถ้าจะเปลี่ยนชื่อ property ใน Notion ต้องไปแก้ค่าคงที่ `PROPS_NOTION` ที่ต้นไฟล์ `Code.gs` ให้ตรงกันด้วย ไม่งั้นระบบจะดึงข้อมูลไม่เจอ `สถานะงาน` ในชุดนี้เป็นชนิด Select; หากเปลี่ยนเป็นชนิด Status ให้แก้ `NOTION_STATUS_PROPERTY_TYPE` เป็น `status` ส่วนสถานะที่อนุญาตให้ส่งกำหนดใน `NOTION_SEND_STATUSES` โดยค่าเริ่มต้นคือ `ยืนยันแล้ว` เท่านั้น

## 5. Deploy Webhook.gs เพื่อจับ Group ID

1. ในหน้า Apps Script editor: **Deploy > New deployment**
2. เลือกประเภท **Web app**
3. ตั้งค่า Execute as: **Me**, Who has access: **Anyone**
4. กด **Deploy** แล้วอนุญาตสิทธิ์ที่ขอ (ครั้งแรกจะมีหน้าเตือน "Google hasn't verified this app" เพราะเป็นสคริปต์ของเราเอง กด Advanced > ไปต่อได้)
5. คัดลอก **Web app URL** ที่ได้ไว้

## 6. ตั้งค่าใน LINE Developers Console

1. เปิด channel ที่สร้างไว้ > แท็บ **Messaging API**
2. วาง Web app URL จากข้อ 5 ลงช่อง **Webhook URL** > กด **Verify** (ควรขึ้นสำเร็จ)
3. เปิด **Use webhook**
4. เปิด **Allow bot to join group chats** (ค่าเริ่มต้นปิดไว้ ถ้าไม่เปิดจะเชิญบอทเข้ากลุ่มไม่ได้)
5. เก็บ **QR code** หรือ **Bot basic ID** ไว้เชิญบอทเข้ากลุ่มในขั้นถัดไป

## 7. เชิญบอทเข้ากลุ่ม + จับ Group ID

1. เพิ่มบอทเป็นเพื่อนแล้วเชิญเข้ากลุ่ม LINE ที่จะใช้ส่งการแจ้งเตือน
2. พิมพ์ข้อความอะไรก็ได้ 1 ข้อความในกลุ่มนั้น (เช่น "test")
3. เปิดชีต Settings กลับมาดู — แถว `line_group_id` ควรถูกเติมค่าให้อัตโนมัติแล้ว
4. ถ้ายังว่างอยู่ ให้เช็คที่ Apps Script editor > **Executions** (แถบซ้าย) ว่ามี error อะไรไหม

## 8. ติดตั้ง trigger และทดสอบ

1. กลับไปที่ชีต รีเฟรชหน้าเว็บ 1 ครั้ง (เพื่อให้เมนูใหม่ขึ้น)
2. จะเห็นเมนู **ระบบแจ้งเตือนปฏิทิน** โผล่ขึ้นมาข้างเมนู Help
3. กด **ติดตั้ง/อัปเดตเวลาส่งอัตโนมัติ** — อนุญาตสิทธิ์ถ้ามีการขอ และกดเมนูนี้ซ้ำทุกครั้งหลังแก้ `notify_time`
4. กด **ทดสอบส่งตอนนี้** เพื่อเช็คว่าทุกอย่างต่อกันครบ ควรเห็นข้อความเด้งเข้ากลุ่ม LINE ทันที

## 9. อัปเดตวันหยุดให้ตรงกับประกาศทางการ

ชีต `Holidays` มีข้อมูลตัวอย่างเบื้องต้นของปี 2569 ใส่ไว้ให้ แต่ **ต้องตรวจสอบกับแหล่งทางการก่อนใช้จริง**:
- สำนักเลขาธิการคณะรัฐมนตรี: https://soc.go.th
- ราชกิจจานุเบกษา (ประกาศฉบับทางการ): https://ratchakitcha.soc.go.th

คณะรัฐมนตรีเคยเพิ่มหรือถอนวันหยุดกรณีพิเศษกลางปีมาแล้วจริง ดังนั้นควรทบทวนชีตนี้อีกครั้งช่วงต้นปีและเมื่อมีข่าวมติ ครม. เรื่องวันหยุดใหม่

## 10. หน้าตั้งค่าแบบ Web App แยกหน้า (ทางเลือกเสริม)

ขั้นตอนนี้ไม่บังคับ — ระบบทำงานได้ครบแล้วตั้งแต่ข้อ 1-9 ผ่านชีตและเมนูใน Sheet โดยตรง ทำข้อนี้เพิ่มถ้าอยากได้หน้าเว็บสวยๆ ที่มี URL ของตัวเอง ไม่ต้องเปิด Google Sheet เพื่อแก้ค่าต่างๆ

**สำคัญ**: หน้านี้ต้องอยู่ **โปรเจกต์ Apps Script ใหม่แยกต่างหาก** จากโปรเจกต์ในข้อ 2 (ไม่ใช่ไฟล์เพิ่มในโปรเจกต์เดิม) เหตุผลอยู่ในคอมเมนต์ด้านบนของ `WebApp.gs` — สรุปสั้นๆ คือ Webhook.gs ต้องเปิดสิทธิ์ "Anyone" (ให้ LINE ยิงเข้ามาได้โดยไม่ต้อง login) ถ้าเอาหน้าตั้งค่ามาไว้โปรเจกต์เดียวกัน สิทธิ์ "Anyone" จะครอบคลุมหน้าตั้งค่าไปด้วย กลายเป็นใครก็เปิดมาแก้ค่าระบบได้โดยไม่ต้อง login

1. ไปที่ [script.google.com](https://script.google.com) > **New project**
2. ลบโค้ดเปล่าเดิม สร้างไฟล์ **Script** ชื่อ `WebApp.gs` วางโค้ดจากไฟล์ที่แนบมาให้
3. สร้างไฟล์ **HTML** ชื่อ `Index` (สำคัญ: ชื่อไฟล์ต้องเป็น `Index` ไม่ใช่ `Index.html` — Apps Script ใส่นามสกุลให้เอง) วางโค้ดจาก `Index.html` ที่แนบมาให้
4. เปิด Google Sheet จากข้อ 1 คัดลอก **Spreadsheet ID** จาก URL (ส่วนระหว่าง `/d/` กับ `/edit`) ไปวางแทนที่ `SPREADSHEET_ID` ในบรรทัดแรกๆ ของ `WebApp.gs`
5. (แนะนำ) จำกัดคนเข้าถึง: **Project Settings > Script Properties > Add script property** ใส่ `ALLOWED_EDITORS` เป็นอีเมล Google คั่นด้วยจุลภาค เช่น `person1@gmail.com,person2@gmail.com`
6. **แชร์ Google Sheet ให้ทุกอีเมลที่ใส่ใน ALLOWED_EDITORS เป็น Editor ด้วย** (ขั้นตอนนี้จำเป็น ไม่ใช่ทางเลือก) — เหตุผล: deployment ข้อ 7 ต้องตั้ง "Execute as: User accessing the web app" ซึ่งแปลว่าสคริปต์ทำงานภายใต้สิทธิ์ของคนที่กำลังเข้าเว็บแต่ละคน ไม่ใช่สิทธิ์ของเจ้าของสคริปต์ ถ้าใครไม่มีสิทธิ์ Editor บนชีตนี้โดยตรง หน้าเว็บจะเปิดไม่ติดหรือบันทึกอะไรไม่ได้เลยแม้จะผ่าน ALLOWED_EDITORS แล้วก็ตาม
7. **Deploy > New deployment > Web app**
   - Execute as: **User accessing the web app** (สำคัญ — ห้ามเลือก "Me" เพราะ `Session.getActiveUser().getEmail()` ที่ใช้เช็ค `ALLOWED_EDITORS` จะได้ค่าว่างเปล่าเสมอถ้า deploy แบบ "Me" ตามเอกสารทางการของ Google ทำให้ล็อกทุกคนออกหมดรวมถึงตัวเอง)
   - Who has access: **Anyone with Google account** (ถ้าใช้ Google Workspace ขององค์กร เลือก **Anyone within [ชื่อองค์กร]** จะปลอดภัยกว่า เพราะจำกัดแค่คนในองค์กรตั้งแต่ระดับ Google เอง)
8. กด **Deploy** อนุญาตสิทธิ์ที่ขอ แล้วคัดลอก **Web app URL** — นี่คือลิงก์หน้าตั้งค่า บันทึกไว้/ปักหมุดได้เลย
9. เปิดลิงก์ทดสอบ — ครั้งแรกที่แต่ละคนเปิด (รวมถึงตัวเอง) จะเจอหน้าขอสิทธิ์ของ Google ให้กด "Allow" (เป็นเรื่องปกติ เพราะ "Execute as: User accessing the web app" ต้องขอสิทธิ์จากทุกคนที่ใช้ ไม่ใช่แค่เจ้าของ) หลังจากนั้นควรเห็นหน้าตั้งค่า พร้อมสถานะล่าสุดที่ดึงมาจากชีต Logs จริง
10. **(แท็บ "รายงานวันลา") สร้าง Notion integration แบบอ่านอย่างเดียว**: ไปที่ [notion.so/my-integrations](https://www.notion.so/my-integrations) > **New integration** ตั้งชื่อ (เช่น "รายงานวันลา (อ่านอย่างเดียว)") > ที่ **Capabilities** ติ๊กเฉพาะ **Read content** (ไม่ติ๊ก Update/Insert) > สร้างแล้วคัดลอก token ไปใส่ Script Properties ของ**โปรเจกต์ WebApp นี้** ชื่อ `NOTION_TOKEN_READONLY`
11. **แชร์ database "ใบลา" ให้ integration ตัวนี้ด้วย**: เปิด database ใบลา > **"..."** > **Connections** > เพิ่ม integration ที่เพิ่งสร้าง (แชร์แบบเดียวกับขั้นตอนที่ทำกับ integration หลักในข้อ 4) — เสร็จแล้วแท็บ "รายงานวันลา" ในหน้าเว็บนี้จะใช้ได้ (เลือกปี/เดือน ดูตารางรายคน×ประเภท ดาวน์โหลด CSV)

หน้านี้แก้ Settings, จัดการ Holidays (เพิ่ม/ลบ), ดู Logs และดู**รายงานวันลา**ได้ (ข้อ 10-11) แต่**ไม่มีปุ่มทดสอบส่งจริง** เพราะตั้งใจไม่เก็บ LINE token ไว้ในโปรเจกต์นี้ (ลดจุดที่ secret รั่วได้ถ้าหน้านี้มีช่องโหว่ในอนาคต) — ส่วนรายงานใช้ token ของ integration ที่ทำได้แค่ "อ่าน" ข้อมูลใบลาเท่านั้น แม้รั่วก็แก่/ส่งข้อความแทนใครไม่ได้ อยากทดสอบส่งจริงยังต้องใช้เมนู "ทดสอบส่งตอนนี้" ในชีตตามหัวข้อที่ 8 ด้านบนเหมือนเดิม หมายเหตุ: หลังเพิ่มรายงาน ทุก editor จะเจอหน้าขออนุญาต Google เพิ่ม 1 ครั้ง (สิทธิ์เชื่อมต่อภายนอกของ UrlFetchApp) — กด Allow ได้ตามปกติ

## 11. ระบบลางานเจ้าหน้าที่ (LIFF + ปุ่มอนุมัติบน LINE)

ต่อยอดจากข้อ 1-10 — เพิ่มการยื่นใบลาผ่านหน้าเว็บ LIFF, การ์ดขออนุมัติส่งตรงถึงผู้อนุมัติรายคน (แชท 1:1 กับ OA ไม่ผ่านกลุ่ม), ปุ่มอนุมัติ/ไม่อนุมัติบนการ์ด, และหัวข้อ "ผู้ลาวันนี้" ในข้อความสรุปเช้า

**ภาพรวมชิ้นส่วน** (ทำตามลำดับ 11.1 → 11.8 ใช้เวลารวม ~1 ชั่วโมง):

| ชิ้น | ทำหน้าที่ | อยู่ที่ไหน |
|---|---|---|
| ชีต `Staff` | ทำเนียบเจ้าหน้าที่ + การผูก LINE userId (เพิ่มอัตโนมัติเมื่อผู้ใช้ลงทะเบียนเอง) | ในสเปรดชีตเดิม (สร้างด้วยเมนู) |
| ชีต `Approvers` | คอนฟิกผู้อนุมัติ: กลุ่มงานไหนใครอนุมัติ ส่งต่อ หัวหน้า สสอ. ไหม (+ เป็นรายการ dropdown กลุ่มงานของฟอร์ม) | ในสเปรดชีตเดิม (สร้างด้วยเมนู ผู้ดูแลกรอก) |
| Notion database "ใบลา" | เก็บใบลาทั้งหมด (source of truth) + audit | Notion workspace เดิม |
| `Leave.gs` | API รับคำขอจาก LIFF + ตรวจ token + อนุมัติผ่านปุ่ม + ยกเลิก/แก้ไขใบลา | โปรเจกต์ Apps Script เดิม (เพิ่มไฟล์) |
| `liff-form/index.html` | หน้าฟอร์มยื่นลา + แท็บ "ของฉัน" (ยอดวันลา + รายการใบลาของตัวเอง แก้ไข/ยกเลิกได้เอง) | โฮสต์บน GitHub Pages |
| LINE Login channel + LIFF app | ให้หน้าฟอร์มรู้ว่าใครเปิด (LINE userId) | LINE Developers Console |

### 11.1 เตรียมชีตและคอนฟิกผู้อนุมัติ

1. เปิดสเปรดชีต > เมนู **ระบบแจ้งเตือนปฏิทิน > เตรียม/ตรวจสอบชีตทั้งหมด** — จะสร้างชีตที่ขาด (Settings/Holidays/Logs/**Approvers**/**Staff**) ใส่หัวตาราง บังคับรูปแบบวันที่เป็น `yyyy-MM-dd` เติมค่าตั้งต้นใน Settings เฉพาะ key ที่ยังไม่มี **และตรวจความพร้อมระบบให้ครบ** (ID ยังเป็นตัวอย่างไหม / token ใน Script Properties ครบไหม / ชื่อกลุ่มงาน-ชื่อคนซ้ำไหม / ผู้อนุมัติที่อ้างในชีต Approvers มีชื่อตรงกับใน Staff ไหม / ตั้งส่งต่อแต่ไม่มีรายชื่อ หัวหน้า สสอ. ไหม / trigger เวลาส่งเช้าตั้งไว้ไหม / สวิตช์ระบบลาเปิดอยู่ไหม) — รันซ้ำกี่ครั้งก็ปลอดภัย ไม่ทับของเดิม และใช้เป็น "เช็คสุขภาพระบบ" เป็นระยะได้เลย
2. **กรอกชีต `Approvers`** — หัวใจของการอนุมัติ กำหนดเองทั้งหมด ไม่มี hardcode:
   - **กลุ่มงาน** — ชื่อกลุ่มงาน (คอลัมน์นี้ยังเป็นรายการ dropdown กลุ่มงานในฟอร์มลงทะเบียนด้วย — ใส่กี่กลุ่มก็ได้)
   - **ผู้อนุมัติ** — "ชื่อ สกุล" ของผู้อนุมัติของกลุ่มนั้น (หลายคนคั่นด้วยจุลภาค ใครกดปุ่มก่อนถือก่อน; ต้องเป็นชื่อที่ลงทะเบียนในระบบแล้ว)
   - **ส่งต่อให้ หัวหน้า สสอ.** — ใส่ `TRUE` ถ้าใบลาของกลุ่มงานนี้ต้องผ่าน หัวหน้า สสอ. อีกชั้น (เว้นว่าง = จบที่ผู้อนุมัติของกลุ่ม)
3. **ใส่รายชื่อ หัวหน้า สสอ.** ในแถว `second_approvers` ของชีต Settings — "ชื่อ สกุล" คั่นจุลภาค (หลายคนได้ ต้องลงทะเบียนในระบบแล้ว)
4. ตรวจ dropdown ของฟอร์มลงทะเบียน: `prefix_options` (คำนำหน้า) และ `position_options` (ตำแหน่ง) ในชีต Settings — แก้รายการให้ตรงหน่วยงานได้เลย ใส่ `อื่นๆ` ไว้ = ฟอร์มเปิดช่องพิมพ์เอง
5. **ชีต `Staff` ไม่ต้องกรอกล่วงหน้า** — แถวเพิ่มอัตโนมัติเมื่อแต่ละคนลงทะเบียนผ่านฟอร์มเอง (คำนำหน้า/ชื่อ/สกุล/กลุ่มงาน/ตำแหน่ง กรอกโดยเจ้าตัว)

กฎที่ระบบใช้จากคอนฟิกนี้: ใบลา → ผู้อนุมัติของกลุ่มงาน → (ถ้ากลุ่มตั้งส่งต่อ) หัวหน้า สสอ. / ผู้ยื่นคือผู้อนุมัติของกลุ่มตัวเอง → ข้ามไป หัวหน้า สสอ. ทันที / **หัวหน้า สสอ. ยื่นเอง → นอกระบบ** (ยื่นช่องทางเดิม) / ผู้อนุมัติของกลุ่มยังไม่ลงทะเบียน → ใบลาขึ้น หัวหน้า สสอ. ทันที หรือเข้ากลุ่มหลักให้ผู้อนุมัติรายอื่นกดแทน

> ย้ายระบบมาจากโครงเดิม? ถ้าชีต Staff เคยกรอกโครง 7 คอลัมน์เดิม (มี "ระดับ") ให้แยกคอลัมน์ชื่อ-สกุลเป็น "ชื่อ"/"สกุล" แล้วลบคอลัมน์ระดับทิ้งเอง (เมนูเตรียมชีตจะไม่ทับข้อมูลที่มีอยู่) และแถว `leave_types_needing_director` ใน Settings ลบได้ (ไม่ใช้แล้ว — กฎส่งต่อย้ายไปอยู่ที่ชีต Approvers รายกลุ่มงานแล้ว)

### 11.2 สร้าง Notion database "ใบลา"

สร้าง database ใหม่ (Full page database) ใน workspace เดิม ตั้งชื่อ property **ตรงตามนี้ทุกตัวอักษร** และ**ชนิดต้องถูก**:

| Property | ชนิด | options / หมายเหตุ |
|---|---|---|
| ผู้ลา | Title | เก็บชื่อเต็ม (คำนำหน้า+ชื่อ-สกุล) |
| กลุ่มงาน | Text | |
| ผู้ยื่น (ระบบ) | Text | ระบบเติม LINE userId — **อย่าแก้** ใช้ push ผลอนุมัติกลับ |
| ประเภทการลา | Select | ลาป่วย, ลากิจ, ลาพักร้อน, ลาคลอด, ลาบวช, อื่นๆ |
| วันที่ลา | Date | ใช้ช่วงวันที่ (End date) สำหรับลาหลายวัน |
| เหตุผล | Text | |
| สถานะ | Select | รอผู้อนุมัติ, รอหัวหน้า สสอ.อนุมัติ, อนุมัติ, ไม่อนุมัติ, ยกเลิก |
| ผู้อนุมัติปัจจุบัน | Text | ระบบเติม JSON ภายใน — **อย่าแก้** |
| บันทึกการอนุมัติ | Text | audit trail ระบบเติม |
| หมายเหตุระบบ | Text | ระบบเติม: ยอดใช้/สิทธิ์ของปี + คำเตือนตามระเบียบ (เกินสิทธิ์, ต้องมีใบรับรองแพทย์ ฯลฯ) — **เพิ่มภายหลังได้** ก่อนเพิ่มระบบจะไม่บันทึกฟิลด์นี้ก็ทำงานได้ |
| ช่วงวัน | Text | เต็มวัน (ค่าเริ่มต้น) / ครึ่งวันเช้า / ครึ่งวันบ่าย — **เพิ่มภายหลังได้** ก่อนเพิ่มระบบจะบันทึกเฉพาะใบเต็มวัน |
| จำนวนวันทำการ | Number | ระบบเติม (ข้ามเสาร์-อาทิตย์และวันหยุดจากชีต Holidays) — ลาครึ่งวัน = 0.5 |

จากนั้น: กด **"..."** > **Connections** > เพิ่ม integration เดิมที่ใช้กับปฏิทินงาน > คัดลอก Database ID จาก URL ไปวางในแถว `leave_database_id` ของชีต Settings

**หลักการคำนวณวันลาตามระเบียบสำนักนายกฯ ว่าด้วยการลาของข้าราชการ** (สรุปสิ่งที่ระบบทำอัตโนมัติ):

- **นับเฉพาะวันทำการ** — ข้ามเสาร์-อาทิตย์และวันหยุดจากชีต Holidays (ใบลาคร่อมวันหยุดนับเฉพาะวันทำการในช่วง)
- **ลาครึ่งวัน** — ป่วย/กิจ/พักผ่อน เลือกได้เมื่อลา 1 วัน (ครึ่งวันเช้า/บ่าย) นับเป็น 0.5 วันทำการ
- **ตรวจสิทธิ์ต่อปีจากใบลาจริงใน Notion** — กิจ 10 วันทำการ/ปี, พักผ่อน 10/ปี, คลอด 90, อุปสมบถ/บวช 15, ช่วยเหลือภริยาคลอด 15 (ลาป่วยไม่จำกัด) — นับรวมใบที่กำลังรออนุมัติ
- **คำเตือนอัตโนมัติ (เตือนอย่างเดียว ไม่บล็อก)**: เกิน/ครบสิทธิ์ปีนี้ · ลาป่วยเกิน 3 วันทำการต้องมีใบรับรองแพทย์ (≥30 วัน ต้องมีทุกครั้ง) · พักผ่อนเกิน 10 วัน/ครั้ง (ต้องเป็นสิทธิ์สะสม ≤45) · ลากิจล่วงหน้าไม่ถึง 3 วันทำการ — คำเตือนแสดงบนฟอร์ม, บันทึกลงใบลา และแสดงในการ์ดให้ผู้อนุมัติเห็น
- **รายการประเภทการลาแก้ได้** ที่คีย์ `leave_type_options` ในชีต Settings — ชื่อที่ตรงตามระเบียบได้การตรวจสิทธิ์อัตโนมัติ ชื่ออื่นยื่นได้แต่ไม่มีการตรวจ

### 11.3 สร้าง LINE Login channel + LIFF app

**สำคัญ: ต้องอยู่ใน provider เดียวกับ Messaging API channel เดิม** เพราะ userId ของคนเดียวกันจะตรงกันทั้งสอง channel เฉพาะใน provider เดียวกันเท่านั้น (ไม่งั้น push ผลอนุมัติกลับหาผู้ยื่นไม่ได้)

1. LINE Developers Console > provider เดิม > **Create a new channel** > เลือกประเภท **LINE Login**
2. เข้า channel > แท็บ **LIFF** > **Add** ตั้งค่าดังนี้:
   - **Endpoint URL**: URL ของหน้าฟอร์มจากข้อ 11.5 (ต้องมี `/` ปิดท้าย เช่น `https://9gotstory.github.io/notion-notify/liff-form/` — ถ้าไม่มี slash GitHub จะ redirect ทำให้ URL ไม่ตรงกับที่ลงทะเบียนและ `liff.init()` พัง)
   - **Size**: Full screen
   - **Scopes**: `profile`, `openid` (ไม่ต้องติ๊ง `chat_message.write`)
   - **Bot link feature**: **Aggressive** — คนที่ยังไม่ได้แอด OA จะเจอหน้าชวนแอดอัตโนมัติตอนเปิดฟอร์ม (จำเป็น เพราะต้องแอดบอทไว้ถึงจะรับ push ผลอนุมัติได้)
   - **Module mode**: ปิด
3. จดค่า 2 อย่าง: **LIFF ID** (เริ่มด้วย `16...`) และ **Channel ID** ของ LINE Login channel (แท็บ Basic)

### 11.4 ตั้งค่าฝั่ง Apps Script

1. Project Settings > Script Properties > เพิ่ม `LOGIN_CHANNEL_ID` = Channel ID จากข้อ 11.3 (ใช้ตรวจว่า access token ที่ส่งมาออกจาก LIFF ของเราจริง กัน token จากแอปอื่น)
2. เพิ่มไฟล์ `Leave.gs` ในโปรเจกต์ และอัปเดต `Code.gs` / `Webhook.gs` / `Tests.gs` ตามไฟล์ใน repository นี้
3. **Deploy > Manage deployments > แก้ไข deployment เดิม (ไอคอนดินสอ) > Version: New version > Deploy** — URL เดิมไม่เปลี่ยน ค่า Webhook URL ที่ตั้งไว้ใน LINE ใช้ได้ตามเดิม **ห้ามลบ deployment นี้อีกต่อไป** เพราะทั้งปุ่มอนุมัติและ API ของหน้า LIFF วิ่งผ่าน URL นี้

### 11.5 โฮสต์หน้าฟอร์มบน GitHub Pages (ค่าตั้งค่าอยู่ใน GitHub Environment)

**ห้ามโฮสต์หน้า LIFF บน Apps Script Web App** — exec URL จะ redirect ไป `*.googleusercontent.com` ทำให้ URL ไม่ตรงกับ endpoint ที่ลงทะเบียน และ `liff.init()` พังทุกครั้ง

หน้าฟอร์มไม่ได้เก็บค่าตั้งค่าในไฟล์ — ใน `liff-form/index.html` มีแค่ placeholder `__LIFF_ID__` / `__API_URL__` และ GitHub Actions (`.github/workflows/deploy-liff-form.yml`) จะเอาค่าจริงจาก **Environment secrets** มาแทนที่ก่อน deploy ขึ้น Pages ทุกครั้งที่ push แก้ไฟล์ใน `liff-form/` — เปลี่ยนค่าได้โดยไม่ต้องแตะโค้ด และค่าถูก mask ใน log ทั้งหมด

**ตั้งค่าครั้งเดียว ตามลำดับนี้:**

1. ถ้า repository นี้เป็น **private** ต้องเลือกก่อน: เปิดเป็น public (ไฟล์ใน repo ไม่มี secret ใดๆ — token ทั้งหมดอยู่ใน Script Properties / Environment เท่านั้น) หรือสร้าง repository สาธารณะใหม่แยกให้ฟอร์มโดยเฉพาะ
2. **Settings > Environments > New environment** ตั้งชื่อ `liff` แล้วเพิ่มค่า 2 ตัว — เป็น **secret** (ถูก mask ใน log แต่เปิดดูค่าซ้ำไม่ได้) หรือ **variable** (เปิดดู/แก้ได้ตลอด) ก็ได้ workflow อ่าน secret ก่อน ถ้าไม่มีค่อยอ่าน variable:
   - `LIFF_ID` = จากข้อ 11.3
   - `API_URL` = Web app URL จากข้อ 11.4 (ลงท้ายด้วย `/exec`)
3. **Settings > Pages > Source: "GitHub Actions"** (สำคัญ — ถ้ายังเป็น "Deploy from a branch" ให้เปลี่ยน ไม่งั้น workflow deploy ไม่ได้ และหน้าที่ถูกเสิร์ฟจะเป็นไฟล์ placeholder ที่ยังไม่ถูกแทนที่ ฟอร์มจะขึ้นว่า "ระบบยังไม่พร้อมใช้งาน")
4. `git push` — Actions จะรันเอง (ดูได้ที่แท็บ Actions) หน้าฟอร์มอยู่ที่ URL เดิม `https://<username>.github.io/notion-notify/liff-form/` และเสิร์ฟเฉพาะหน้าฟอร์ม ไม่โฮสต์ไฟล์ .gs อื่นของ repo

**แก้ค่าภายหลัง** (เช่น เปลี่ยน deployment ของ Apps Script): แก้ใน Settings > Environments > liff แล้วไปแท็บ Actions > Deploy LIFF form > Run workflow — ไม่ต้องแก้โค้ดและไม่ต้อง commit

**ทดสอบในเครื่อง**: copy `liff-form/index.html` ไปไฟล์อื่น แทนที่ placeholder ด้วยค่าจริงแล้วเปิดผ่าน local server ก็ได้ (อย่า commit ไฟล์ที่ใส่ค่าจริงเด็ดขาด)

**การแก้สไตล์หน้าฟอร์ม**: หน้าใช้ **Tailwind v4** — คลาสอยู่ใน `index.html` ตามปกติ ส่วนสี/ฟอนต์ของธีมประกาศที่ `liff-form/src/styles.css` ผ่าน `@theme` (ธรรมเนียม CSS-first ของ v4) **ไม่ต้อง build เองตอนแก้** — push แล้ว GitHub Actions คอมไพล์ CSS ให้อัตโนมัติทุกครั้ง (ผลลัพธ์เป็น `styles.css` same-origin กับหน้า ไม่พึ่ง CDN ภายนอก เพราะเครือข่ายองค์กรบางแห่ง reset การเชื่อมต่อไป `cdn.tailwindcss.com`) ถ้าอยากดูผลในเครื่องก่อน:

```bash
cd liff-form
npm install        # ครั้งแรกเท่านั้น
npm run build:css  # สร้าง styles.css สำหรับเปิดผ่าน local server (ไฟล์นี้ถูก gitignore)
```

### 11.6 ปุ่ม "ยื่นลา" บน OA (Rich Menu)

ไปที่ [LINE Official Account Manager](https://manager.line.biz/) > channel ของ OA > **Rich Menu** สร้างเมนูใหม่: พื้นที่แตะเต็มความกว้าง 1 ปุ่ม ข้อความ "ยื่นลา 🗓️" Action เลือก **ลิงก์** แล้ววาง URL ของ LIFF (`https://liff.line.me/<LIFF_ID>`) จากนั้นตั้งเป็น default rich menu — ทำผ่านหน้าเว็บทั้งหมด ไม่ต้องเขียนโค้ด (จะแชร์ลิงก์ปักหมุดในกลุ่มแทนก็ได้ ใช้งานได้เหมือนกัน)

### 11.7 การใช้งานครั้งแรกของแต่ละคน

เจ้าหน้าที่แตะปุ่ม "ยื่นลา" > แอด OA เป็นเพื่อน (เด้งให้อัตโนมัติ) > กรอกฟอร์มลงทะเบียน **คำนำหน้า (dropdown) / ชื่อ / สกุล / กลุ่มงาน (dropdown) / ตำแหน่ง (dropdown)** > หน้ายืนยันข้อมูล > เริ่มยื่นลาได้เลย ต่อจากนั้นทุกครั้งเปิดปุ๊บรู้จักทันทีไม่ต้องกรอกอะไรซ้ำ

**บทบาทผู้ดูแล**: ทุกครั้งที่มีคนลงทะเบียน ระบบแจ้งเข้ากลุ่มหลักพร้อมชื่อใน LINE ของบัญชีนั้น — ช่วยกันสังเกตว่ามีคนลงทะเบียนชื่อคนอื่นหรือไม่ (ชื่อเดียวกันลงทะเบียนซ้ำไม่ได้ และหลังจากนี้ให้อย่าลืมเติมชื่อผู้อนุมัติลงชีต Approvers ตามคนที่ลงทะเบียนแล้ว)

### 11.8 ทดสอบและบำรุงรักษา

- **เปิด/ปิดระบบลา**: กดเมนู **"เปิด/ปิดระบบลา"** เพื่อสลับสถานะ (หรือแก้คีย์ `leave_system_enabled` ในชีต Settings เป็น `FALSE`/`TRUE` เองก็ได้) — เมื่อปิด: การลงทะเบียนใหม่และการยื่นใบลาถูกปฏิเสธพร้อมข้อความแจ้ง (แก้ข้อความได้ที่คีย์ `leave_closed_message` เช่นระบุช่วงเวลาปิด+ผู้ติดต่อ) ส่วน**ใบลาเดิมที่รออนุมัติยังกดปุ่มได้ตามปกติ** และสรุปเช้ายังแสดงผู้ลาที่อนุมัติแล้ว — ทุกการสลับถูกบันทึกในชีต Logs
- **เปิด/ปิดการอนุมัติใบลา (โหมดแจ้งลาอัตโนมัติ)**: กดเมนู **"เปิด/ปิดการอนุมัติใบลา"** (หรือแก้คีย์ `leave_approval_enabled` เป็น `FALSE`) — เมื่อปิด: การยื่นลา**บันทึกเป็น "อนุมัติ" ทันที**โดยไม่ต้องมีใครกดปุ่ม และไม่ต้องมีคอนฟิกผู้อนุมัติในชีต Approvers เลย ระบบแจ้งการ์ด (ไม่มีปุ่ม) เข้ากลุ่มหลัก + ยืนยันกลับผู้ยื่น และใบลาขึ้นสรุปเช้า "ผู้ลาวันนี้" ทันที — เหมาะช่วงที่ไม่ต้องการกระบวนการอนุมัติ ใบลาเดิมที่รออนุมัติอยู่ยังกดปุ่มได้จนจบ
- **สรุปวันลารายเดือน**: วันทำการแรกของแต่ละเดือน ระบบแนบสรุปใบลาที่อนุมัติของเดือนก่อนหน้าท้ายข้อความสรุปเช้า (รายคน×ประเภท + รวมทั้งเดือน) — รวมอยู่ในข้อความเดียวกัน จึงไม่ใช้โควตา LINE เพิ่มแม้แต่ข้อความเดียว ปิดได้ที่คีย์ `monthly_leave_summary_enabled` (FALSE = ปิด) เดือนที่ไม่มีใครลาเลยจะไม่มีส่วนสรุป (ระบบจำไว้ใน Script Properties คีย์ `last_monthly_leave_summary` ว่าเดือนไหนจัดไปแล้ว) / ทดสอบหน้าตาได้ด้วยเมนู **"ทดสอบสรุปรายเดือน"** โดยไม่ต้องรอข้ามเดือน
- **ทดสอบหน้าตาการ์ด**: เมนู "ทดสอบการ์ดใบลา" ส่งการ์ดตัวอย่างเข้ากลุ่มหลัก (ปุ่มบนการ์ดตัวอย่างกดแล้วไม่มีผล)
- **ทดสอบ E2E**: ยื่นลาจริงด้วยบัญชีทดสอบ 1 ใบ > เช็คว่าการ์ดไปถึงหัวหน้า 1:1 > กดปุ่ม > เช็คสถานะ+บันทึกการอนุมัติใน Notion > ผู้ยื่นได้รับผล > เช้าวันทำการถัดไปขึ้น "ผู้ลาวันนี้" ในการ์ดปฏิทิน
- **เปลี่ยนบัญชี LINE / กรอกข้อมูลผิด**: ผู้ดูแลลบแถวนั้นในชีต Staff (หรือล้างคอลัมน์ LINE User ID) แล้วให้เจ้าของเปิดฟอร์มลงทะเบียนใหม่
- **ยกเลิก/แก้ไขใบลาที่ยื่นไปแล้ว**: ผู้ยื่นทำเองได้ในแท็บ **"ของฉัน"** ของฟอร์มลา (ดูหัวข้อ 11.10) — **ยกเลิก**ได้ทั้งใบรออนุมัติและใบอนุมัติแล้วที่ยังไม่ถึงวัน (ระบบแจ้งผู้อนุมัติ 1:1 อัตโนมัติ และคืนยอดสิทธิ์ทันที) / **แก้ไข**ได้เฉพาะใบที่ยังรออนุมัติ (แก้แล้วระบบคำนวณวันทำการใหม่ ส่งการ์ดขออนุมัติใหม่ และบันทึกการแก้ไขลง audit) — เหลือเพียงใบที่วันที่ผ่านมาแล้วหรือใบที่อนุมัติแล้วอยากแก้วันที่ ที่ต้องให้ผู้ดูแลแก้สถานะ/ข้อมูลใน Notion เอง
- **Unit tests**: เมนู "รัน Unit Tests" ครอบคลุมตัวสร้าง payload/การ์ด/กฎวันทำการ/บันไดผู้อนุมัติ/กติกาแก้ไข-ยกเลิก/สรุปรายเดือน ทั้งหมด

### 11.9 หน้าดูตารางงาน (`/schedule/`)

หน้าเว็บแยกจากฟอร์มลา ดูปฏิทินการปฏิบัติงานย้อนหลัง 1 เดือน–ล่วงหน้า 6 เดือน แบบ **สองโหมด**: สาธารณะ (มีลิงก์ก็ดูได้ — เห็นงาน/เวลา/สถานที่) และเจ้าหน้าที่ (เข้าสู่ระบบ LINE + ลงทะเบียนในชีต Staff แล้ว — เห็น**ผู้รับผิดชอบ/รายละเอียด/หมายเหตุ**เพิ่ม) รายการที่แสดงใช้ whitelist สถานะเดียวกับข้อความเช้า (เฉพาะ "ยืนยันแล้ว")

**ใช้งานได้ทันทีแบบสาธารณะ** หลัง push (ไม่ต้องตั้งค่าอะไรเพิ่ม) ที่ `https://<user>.github.io/notion-notify/schedule/` — ส่วนปุ่มล็อกอินเจ้าหน้าที่ต้องสร้าง **LIFF app ตัวที่สอง** (LIFF ผูกกับ URL เดียวต่อแอป ฟอร์มลาใช้ URL ของตัวเองไปแล้ว):

1. LINE Developers Console > provider เดิม > **LINE Login channel เดิม** > tab LIFF > Add: Endpoint = `https://<user>.github.io/notion-notify/schedule/` (**ปิดท้ายด้วย `/`**), Size **Full**, Bot link = **Off หรือ Normal** (หน้านี้ไม่จำเป็นต้องบังคับแอดเพื่อน — ต่างจากฟอร์มลา)
2. คัดลอก LIFF ID ใหม่ ไปใส่ GitHub Environment "liff" เป็น **`SCHEDULE_LIFF_ID`** (secret หรือ variable ก็ได้)
3. push อะไรก็ได้ใน `schedule/` หรือกด workflow ด้วยตัวเอง (Actions > Deploy LIFF form > Run workflow) แล้วทดสอบ: เปิดในแอป LINE ต้องขึ้นป้าย "✓ มุมมองเจ้าหน้าที่" / เปิดด้วย Chrome ต้องเป็น "มุมมองทั่วไป" พร้อมปุ่มล็อกอิน
4. เอาลิงก์ `https://liff.line.me/<SCHEDULE_LIFF_ID>` ไปทำปุ่มที่สองบน Rich Menu (เช่น "ตารางงาน 📅") หรือแชร์ URL ตรง `.io/notion-notify/schedule/` ปักหมุดในกลุ่มก็ได้

**หมายเหตุ**: ถ้าไม่ต้องการโหมดสาธารณะ (อยากบังคับล็อกอินก่อนเห็นข้อมูลเลย) หรืออยากซ่อนฟิลด์อื่นเพิ่ม แก้ที่ `toScheduleItem_` ใน Code.gs จุดเดียว

### 11.10 แท็บ "ของฉัน" — ยอดวันลา + แก้ไข/ยกเลิกใบลาด้วยตัวเอง

ในหน้าฟอร์มลาเดิม (`liff-form/`) เพิ่มแถบแท็บ 2 แท็บที่โหลดเพิ่มอัตโนมัติเมื่อ push ขึ้น GitHub Pages — ไม่ต้องสร้าง LIFF app ใหม่ ไม่ต้องตั้งค่าอะไรเพิ่ม:

- **แท็บ "ยื่นลา"** = ฟอร์มเดิมทุกอย่าง
- **แท็บ "ของฉัน"** มี 2 ส่วน:
  - **ยอดวันลาปีนี้** — แต่ละประเภท: ใช้ไปแล้ว / สูงสุด / **เหลือ** (คำนวณสดจากใบลาจริงใน Notion ทุกครั้งที่เข้าแท็บ — ยอด "ใช้ไปแล้ว" รวมใบที่กำลังรออนุมัติด้วย เพื่อกันยื่นพร้อมกันหลายใบจนทะลุสิทธิ์โดยไม่รู้ตัว เหมือนกติกาตอนยื่น)
  - **ใบลาของฉันปีนี้** — รายการทุกใบทุกสถานะพร้อมป้ายสี (รอ/อนุมัติ/ไม่อนุมัติ/ยกเลิก) และปุ่มตามสถานะ:
    - **แก้ไข** (เฉพาะใบรออนุมัติ): เปิดฟอร์มเดิมพร้อมข้อมูลเดิมให้แก้ → บันทึกแล้วระบบคำนวณวันทำการ/คำเตือนใหม่ ตั้งสถานะกลับ "รอผู้อนุมัติ" (รันผู้อนุมัติใหม่จากชีต Approvers ล่าสุด) ส่งการ์ดใหม่ให้ผู้อนุมัติ และเติมบรรทัด audit "ผู้ยื่นแก้ไขใบลา" ลงใบเดิมใน Notion
    - **ยกเลิก** (ใบรออนุมัติ + ใบอนุมัติแล้วที่ยังไม่ถึงวัน): ไม่ต้องขออนุมัติยกเลิกซ้ำ — ใบรอถูกถอนจากผู้อนุมัติที่ค้าง (แจ้ง 1:1), ใบอนุมัติแล้วแจ้งผู้อนุมัติของกลุ่มงานปัจจุบัน 1:1 ให้ทราบ, ปุ่มบนการ์ด LINE เก่าใช้ไม่ได้ทันที และยอดสิทธิ์คืนทันที / ใบที่วันที่ผ่านมาแล้วยกเลิกเองไม่ได้ (กันแก้ประวัติย้อนหลัง — ติดต่อผู้ดูแลแทน)
- การแจ้งทั้งหมดเป็นแชทส่วนตัว 1:1 เท่านั้น ไม่มีอะไรเกี่ยวกับใบลาของแต่ละคนหลุดเข้ากลุ่มหลัก
- โหมดต่างๆ: ปิดระบบลา (`leave_system_enabled` = FALSE) ยังยกเลิกได้ (เหมือนปุ่มอนุมัติที่ยังใช้ได้) แต่แก้ไขไม่ได้เพราะการแก้ = การยื่นใหม่ / โหมดปิดการอนุมัติ: แก้ไขแล้วคงสถานะอนุมัติและแจ้งการ์ดเข้ากลุ่มหลักตามธรรมเนียมของโหมดนั้น

**รายงานสรุปรายเดือน/รายปี**: ดูได้ 2 ที่ — (1) สรุปย่อเดือนที่ผ่านมาแนบท้ายข้อความสรุปเช้าวันทำการแรกของแต่ละเดือน (ปิด/เปิดได้ที่ `monthly_leave_summary_enabled` ดูหัวข้อ 11.8) (2) ตารางละเอียดรายคน×ประเภท เลือกปี/เดือน + ดาวน์โหลด CSV ที่แท็บ **"รายงานวันลา"** ของหน้าเว็บตั้งค่า (ตั้งค่าครั้งเดียวตามข้อ 10.10-11)

## ข้อจำกัดที่ควรรู้

- **ความแม่นยำของเวลา**: ระบบสร้าง one-time trigger ตาม `notify_time` แล้วนัดวันถัดไปใหม่หลังทำงานเสร็จ Apps Script อาจเริ่มช้ากว่าเวลาที่นัดเล็กน้อยตามภาระของระบบ แต่จะไม่ส่งย้อนหลังทันทีเพียงเพราะติดตั้ง trigger หลังเวลาที่กำหนด
- **เมื่อเปลี่ยนเวลา**: หลังแก้ `notify_time` ต้องกดเมนู **ติดตั้ง/อัปเดตเวลาส่งอัตโนมัติ** เพื่อยกเลิกนัดเดิมและสร้างนัดใหม่
- **Webhook ไม่ verify signature**: Apps Script เข้าถึง header `X-Line-Signature` ไม่ได้ จึงใช้ชั้นป้องกันแทน 3 ชั้น (อธิบายใน `Webhook.gs`) — ทุกคำขอจาก LIFF ตรวจ access token กับ api.line.me จริง / ปุ่มอนุมัติตรวจ userId ผู้กดกับ "ผู้อนุมัติปัจจุบัน" ใน Notion / dedup ด้วย webhookEventId — ตั้งแต่มีระบบลางาน deployment นี้จึงต้องค้างไว้ถาวร (ไม่ใช่แค่จับ Group ID แล้วปิดตามคำแนะนำเดิม)
- **ระบบลางาน — ความเป็นส่วนตัว**: การ์ดขออนุมัติและผลการอนุมัติเป็นแชทส่วนตัวระหว่าง OA กับรายบุคคล สรุปเช้าแสดงเฉพาะใบลาที่**อนุมัติแล้ว** (ใบที่ยังรอพิจารณาไม่เปิดเผย) ยกเว้นกรณี fallback ที่ไม่มีผู้อนุมัติรายคน การ์ดจะเข้ากลุ่มหลัก
- **ระบบลางาน — คนนอก**: ใครเปิดลิงก์ฟอร์มได้ก็ลงทะเบียนตัวเองได้ (เพราะไม่มีทำเนียบล่วงหน้าให้เทียบ) — ชดเชยด้วย 3 ชั้น: ชื่อ-สกุลเดียวกันลงทะเบียนซ้ำไม่ได้ / ทุกการลงทะเบียนแจ้ง "ชื่อใน LINE ของบัญชีใด ลงทะเบียนเป็นใคร" เข้ากลุ่มหลักให้ผู้ดูแลเห็นทันที / ผู้อนุมัติเห็นชื่อผู้ยื่นในการ์ดก่อนกดอนุมัติเสมอ (ผู้อนุมัติที่ยังไม่ลงทะเบียนก็จะไม่ถูกอ้างเป็นผู้อนุมัติ)
- **ระบบลางาน — ผอ. ลาเอง**: ยื่นนอกระบบ (ระบบจะปฏิเสธและแจ้งผู้ยื่นตาม) เช่นเดียวกับกรณีที่ต้องการผู้อนุมัติแทน ผอ. เมื่อ ผอ. ไม่อยู่
- **ปุ่ม "ไม่อนุมัติ" ไม่มีช่องเหตุผล**: ผู้อนุมัติแจ้งผู้ยื่นตัวต่อตัว หรือจดหมายเหตุใน Notion เอง
- **ถ้าบอทถูกลบออกจากกลุ่ม**: ต้อง deploy Webhook.gs ใหม่ (หรือเปิด deployment เดิมกลับมา) แล้วทำขั้นตอนที่ 7 ซ้ำเพื่อจับ Group ID ใหม่
- **หน้า Web App ตั้งค่า (ข้อ 10)**: ถ้าไม่ได้ตั้ง `ALLOWED_EDITORS` ไว้ ใครก็ตามที่ผ่านเงื่อนไข "Who has access" ตอน deploy จะเข้ามาแก้ Settings/Holidays ได้ทั้งหมด ควรตั้งค่านี้ตั้งแต่แรก และทบทวนรายชื่อเป็นระยะ — และอย่าลืมแชร์ Sheet เป็น Editor ให้ทุกคนในรายชื่อนั้นด้วยตามข้อ 6 ไม่งั้นเข้าหน้าเว็บได้แต่ใช้งานจริงไม่ได้
- **งานที่ตั้งเป็นช่วงหลายวัน (มีวันเริ่ม-วันสิ้นสุดใน Notion)**: ตัว filter ที่ใช้ตอนนี้เช็คจากวันเริ่มเป็นหลัก ยังไม่ยืนยัน 100% ว่า Notion จะรวมงานแบบนี้ในการแจ้งเตือนของทุกวันในช่วงหรือแค่วันแรกวันเดียว — ถ้าเจอว่างานหลายวันหายไปจากการแจ้งเตือนวันถัดๆ ไป ให้แจ้งเพื่อปรับ filter เพิ่ม
- **ยกเลิกแข่งกับกดอนุมัติ**: การยกเลิก/แก้ไขและการกดปุ่มอนุมัติใช้ lock ตัวเดียวกันเพื่อไม่ให้ทำพร้อมกันจนสถานะเพี้ยน แต่เหลือช่องเล็กๆ ระดับวินาที (ผู้อนุมัติกดปุ่มไปแล้วสักพักก่อนคำขอยกเลิกมาถึง) ซึ่งเป็นคลาสเดียวกับการกดอนุมัติซ้ำสองปุ่มพร้อมกันที่ระบบยอมรับอยู่แล้ว — ปุ่มบนการ์ดเก่าจะตอบ "ใบลานี้ดำเนินการไปแล้ว" เสมอ
- **สรุปรายเดือน/รายงานวันลานับตามวันเริ่มของใบ**: ใบลาคร่อมเดือน (เช่น 30–2 ของเดือนถัดไป) นับรวมในเดือนที่เริ่มเท่านั้น ไม่หักแยกข้ามเดือน — ตรงกับวิธีนับยอดสิทธิ์ต่อปีของระบบ
- **ผู้ใช้ที่เปิดหน้าฟอร์มลาค้างไว้ก่อนอัปเดตระบบ**: จะยังเห็นหน้าเดิมไม่มีแท็บ "ของฉัน" จนกว่าจะปิดแล้วเปิดลิงก์ใหม่ (หน้าเก่าเรียกแต่ API เดิมจึงใช้งานต่อได้ปลอดภัย ไม่ error)
- **Notion API rate limit**: อยู่ที่ประมาณ 3 request/วินาทีโดยเฉลี่ยต่อ integration ระบบนี้ยิงจริงแค่วันละ 1-2 ครั้ง (query + ทดสอบเป็นบางครั้ง) จึงไม่มีทางแตะ limit นี้เลย
