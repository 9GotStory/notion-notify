# คู่มือติดตั้งระบบแจ้งเตือนปฏิทินงานเข้า LINE (Apps Script + Google Sheet)

ทำตามลำดับนี้ทีละขั้น ใช้เวลารวมประมาณ 20-30 นาที

> **ภาพรวมโครงสร้าง repo** (โปรเจกต์ Apps Script 2 ตัว + เว็บ 1 กลุ่ม อยู่ในโฟลเดอร์เดียวกันได้อย่างไร) ดูที่ [README.md](README.md) — พร้อมตาราง "แก้อะไรแล้ว push/deploy ยังไง" และตำแหน่งเก็บ secrets ทั้งหมด

> **กำลังเตรียมเปิดใช้งานจริง?** ทำ installation ในเอกสารนี้ให้ครบ แล้วปิด acceptance gates และทำ Go/No-Go ตาม [PRODUCTION-HANDOFF.md](PRODUCTION-HANDOFF.md) ผู้ใช้ ผู้อนุมัติ และผู้ดูแลควรอ่าน [USER-ADMIN-GUIDE.md](USER-ADMIN-GUIDE.md) ก่อนเริ่มใช้งาน

## 1. เตรียม Google Sheet จาก template

ทางเลือก A — ใช้ template (สะดวกสุดสำหรับติดตั้งใหม่):

1. เปิด Google Drive > New > File upload > อัปโหลด `notify-system-template.xlsx`
2. คลิกขวาไฟล์ที่อัปโหลด > Open with > Google Sheets (หรือเปิดไฟล์แล้วเลือก File > Save as Google Sheets)
3. จะได้ Google Sheet ที่มี 3 แท็บ: `Settings`, `Holidays`, `Logs`

ทางเลือก B — สเปรดชีตเปล่า: ข้ามขั้นตอนนี้ไปทำข้อ 2 ตามปกติ แล้วรันเมนู **เตรียม/ตรวจสอบชีตทั้งหมด** (`setupSheet`) ครั้งแรก — ระบบจะสร้างชีตทั้ง 4 พร้อมหัวตาราง ค่าตั้งต้น และรูปแบบวันที่ให้เอง (template จึงเป็นแค่ทางลัด ไม่ใช่ข้อบังคับ)

## 2. เปิด Apps Script (ผูกกับชีตนี้โดยตรง)

1. ในชีตที่เพิ่งสร้าง ไปที่เมนู **Extensions > Apps Script**
2. จะได้โปรเจกต์ Apps Script ใหม่ที่ผูกกับสเปรดชีตนี้อัตโนมัติ (โค้ดจึงเรียก `SpreadsheetApp.getActive()` ได้เลย ไม่ต้องตั้ง Spreadsheet ID เอง)
3. โค้ดทั้งหมดของโปรเจกต์นี้อยู่ใน `apps/main/` (13 ไฟล์ `.gs` + `appsscript.json`) — ติดตั้งด้วย clasp ง่ายสุด: สร้าง `.clasp.json` ใน `apps/main/` ใส่ scriptId ของโปรเจกต์นี้ แล้วรัน `scripts/push-main.sh` (หรือ copy ไฟล์ทุกไฟล์จาก `apps/main/` ขึ้นโปรเจกต์ด้วยมือก็ได้ผลเหมือนกัน)

## 3. ตั้งค่า Secret ใน Script Properties

อย่าใส่ Token ในชีต — ไปที่ **Project Settings** (ไอคอนเฟือง) > **Script Properties** > **Add script property**

| Property | Value |
|---|---|
| `LINE_CHANNEL_ACCESS_TOKEN` | Channel access token จาก LINE Developers Console |
| `NOTION_TOKEN` | Internal Integration Secret จาก Notion (ดูวิธีสร้างในข้อ 4) |
| `ADMIN_TOKEN` | ค่าสุ่มอย่างน้อย 32 ตัวอักษร — ต้องเป็นค่าเดียวกับโปรเจกต์ webapp เพื่อให้ Admin เปลี่ยนผู้อนุมัติ/ปรับผลลาใช้จริงผ่าน backend หลัก |
| `ALLOW_LEGACY_DIRECT` | `TRUE` — เปิด direct mode ตามความเสี่ยงที่เจ้าของระบบยอมรับ |
| `LOGIN_CHANNEL_ID` | Channel ID ของ LINE Login — จำเป็นและ fail-closed หากไม่มี |
| `GATEWAY_SHARED_SECRET` | secret ร่วมกับ gateway สำหรับ LINE webhook (ทางเลือกแนะนำ) |
| `ALLOW_UNSIGNED_LINE_WEBHOOK` | `TRUE` เฉพาะข้อยกเว้น direct LINE webhook ชั่วคราว; ไม่ต้องตั้งเมื่อใช้ gateway |

(ยังไม่มี LINE channel? สร้างที่ [LINE Developers Console](https://developers.line.biz/console/) > สร้าง Provider > สร้าง Messaging API channel)

## 4. สร้าง Notion Integration แล้วเชื่อมกับ database

1. ไปที่ [notion.so/my-integrations](https://www.notion.so/my-integrations) > **New integration**
2. ตั้งชื่อ (เช่น "ระบบแจ้งเตือนปฏิทิน") เลือก workspace ที่มี database "ปฏิทินการปฏิบัติงาน" อยู่ > **Submit**
3. คัดลอก **Internal Integration Secret** ไปใส่ `NOTION_TOKEN` ตามตารางข้อ 3 ด้านบน
4. เปิดหน้า database "ปฏิทินการปฏิบัติงาน" ใน Notion จริง กดปุ่ม **"..."** มุมขวาบน > **Connections** > เพิ่ม integration ที่เพิ่งสร้าง — **ขั้นตอนนี้จำเป็น ไม่ใช่ทางเลือก** ต่อให้ token ถูกต้อง แต่ถ้าไม่แชร์ database ให้ integration ตรงนี้ API จะตอบ "object not found" เสมอ
5. คัดลอก **Database ID** จาก URL ของหน้า database (ส่วนของรหัสยาวๆ ก่อนเครื่องหมาย `?` ถ้ามี) ไปวางในชีต Settings แถว `notion_database_id`

โค้ดอ้างอิงชื่อ property ของ database ตรงๆ (งาน, วันที่ - เวลา, สถานะงาน, ผู้รับผิดชอบ, สถานที่, รายละเอียด, หมายเหตุ) — ถ้าจะเปลี่ยนชื่อ property ใน Notion ต้องไปแก้ค่าคงที่ `PROPS_NOTION` ที่ต้นไฟล์ `apps/main/Notion.gs` ให้ตรงกันด้วย ไม่งั้นระบบจะดึงข้อมูลไม่เจอ `สถานะงาน` ในชุดนี้เป็นชนิด Select; หากเปลี่ยนเป็นชนิด Status ให้แก้ `NOTION_STATUS_PROPERTY_TYPE` เป็น `status` ส่วนสถานะที่อนุญาตให้ส่งกำหนดใน `NOTION_SEND_STATUSES` โดยค่าเริ่มต้นคือ `ยืนยันแล้ว` เท่านั้น

**ผู้รับผิดชอบกับระบบเตือน "คนลาชนงาน"**: ใส่ชื่อใน property ผู้รับผิดชอบเป็น **"ชื่อจริง" ตรงกับทำเนียบ Staff** (เช่น `ธนกร`) — ระบบใช้จับคู่กับใบลาเพื่อเตือน 3 จุด: หน้าตารางงาน (⚠ บนการ์ดงาน), ตอนยื่นใบลา (คำเตือนบนฟอร์มและการ์ดอนุมัติ), และสรุปเช้า (ส่วน "งานที่ผู้รับผิดชอบกำลังลา" วันนั้น) — ใส่ชื่อเล่น/สะกดต่างจากทำเนียบจะไม่ถูกจับคู่ (ไม่ error เพียงแต่ไม่เตือน) ค่าพิเศษ **`ทุกคน`** หมายถึงมอบหมายทั้งหน่วยงาน = มีใครลาก็ถูกเตือน

## 5. Deploy Apps Script backend

1. ในหน้า Apps Script editor: **Deploy > New deployment**
2. เลือกประเภท **Web app**
3. ตั้งค่า Execute as: **Me**, Who has access: **Anyone**
4. กด **Deploy** แล้วอนุญาตสิทธิ์ที่ขอ (ครั้งแรกจะมีหน้าเตือน "Google hasn't verified this app" เพราะเป็นสคริปต์ของเราเอง กด Advanced > ไปต่อได้)
5. คัดลอก **Web app URL** ที่ได้ไว้ตั้ง GitHub Environment ชื่อ `API_URL` และใช้เป็น LINE Webhook URL

## 6. ตั้งค่า LINE ให้เรียก Apps Script โดยตรง

1. ยืนยันว่า Script Property `ALLOW_LEGACY_DIRECT=TRUE` สำหรับหน้าเว็บ และเลือก transport ของ LINE webhook: แนะนำ gateway ที่ตรวจลายเซ็น; หากยอมรับความเสี่ยง direct webhook ชั่วคราวต้องตั้ง `ALLOW_UNSIGNED_LINE_WEBHOOK=TRUE` แยกต่างหาก
2. เปิด channel ที่สร้างไว้ > แท็บ **Messaging API**
3. วาง Web app URL `/exec` จากข้อ 5 ลงช่อง **Webhook URL** > กด **Verify**
4. เปิด **Use webhook** และ **Allow bot to join group chats**
5. เก็บ **QR code** หรือ **Bot basic ID** ไว้เชิญบอทเข้ากลุ่มในขั้นถัดไป

## 7. เชิญบอทเข้ากลุ่ม + จับ Group ID

1. เพิ่มบอทเป็นเพื่อนแล้วเชิญเข้ากลุ่ม LINE ที่จะใช้ส่งการแจ้งเตือน
2. ใน Google Sheet กดเมนู **ระบบแจ้งเตือนปฏิทิน > สร้างรหัสจับคู่กลุ่ม LINE**
3. ส่งข้อความ `เชื่อมกลุ่ม <รหัส>` ตามที่เมนูแสดงในกลุ่มเป้าหมายภายใน 10 นาที รหัสใช้ได้ครั้งเดียว
4. เปิดชีต Settings กลับมาดู — แถว `line_group_id` ควรถูกเติมแล้ว ข้อความทั่วไปหรือข้อความจากกลุ่มอื่นจะเปลี่ยนค่านี้ไม่ได้
5. ถ้ายังว่าง ให้ตรวจ Apps Script **Executions**, transport ของ webhook และเวลาหมดอายุของรหัส แล้วสร้างรหัสใหม่

## 8. ติดตั้ง trigger และทดสอบ

1. กลับไปที่ชีต รีเฟรชหน้าเว็บ 1 ครั้ง (เพื่อให้เมนูใหม่ขึ้น)
2. จะเห็นเมนู **ระบบแจ้งเตือนปฏิทิน** โผล่ขึ้นมาข้างเมนู Help
3. กด **ติดตั้ง/อัปเดตเวลาส่งอัตโนมัติ** — อนุญาตสิทธิ์ถ้ามีการขอ และกดเมนูนี้ซ้ำทุกครั้งหลังแก้ `notify_time`
4. กด **ทดสอบส่งตอนนี้** เพื่อตรวจสอบการเชื่อมต่อ ควรเห็นข้อความในกลุ่ม LINE ทันที
5. ตรวจค่า `logs_retention_days` ในชีต Settings หรือหน้า Admin > **ระบบ > เก็บประวัติ Logs** — ค่าเริ่มต้น `90` วัน ปรับได้ `30-3650` วัน

เมนูติดตั้งเวลาจะสร้าง trigger แยกสำหรับลบเฉพาะแถวในชีต `Logs` ที่เก่ากว่ากำหนดวันละครั้ง แม้ปิดการส่งข้อความเช้า โดยไม่แตะ `AuditLog` หรือ `SecurityEvents` หาก cleanup ไม่สำเร็จ ระบบจะแสดงเป็นงานล้มเหลวใน Apps Script Executions แต่ไม่กระทบ trigger แจ้งเตือนหรือระบบลา

## 9. อัปเดตวันหยุดให้ตรงกับประกาศทางการ

ชีต `Holidays` มีข้อมูลตัวอย่างเบื้องต้นของปี 2569 ใส่ไว้ให้ แต่ **ต้องตรวจสอบกับแหล่งทางการก่อนใช้จริง**:
- สำนักเลขาธิการคณะรัฐมนตรี: https://soc.go.th
- ราชกิจจานุเบกษา (ประกาศฉบับทางการ): https://ratchakitcha.soc.go.th

คณะรัฐมนตรีเคยเพิ่มหรือถอนวันหยุดกรณีพิเศษกลางปีมาแล้วจริง ดังนั้นควรทบทวนชีตนี้อีกครั้งช่วงต้นปีและเมื่อมีข่าวมติ ครม. เรื่องวันหยุดใหม่

## 10. หน้าตั้งค่าผู้ดูแล (SPA บน GitHub Pages + JSON API แยกโปรเจกต์)

> โปรเจกต์ Apps Script แยกเป็น **JSON API** ส่วนหน้า UI อยู่ที่ `web/admin/` และ deploy ผ่าน GitHub Actions ห้ามบันทึก script ID, deployment URL หรือ Spreadsheet ID ของ production ลง repository

โครงสร้าง: หน้า UI เป็น SPA บน GitHub Pages เรียก Apps Script webapp ด้วย GET โดยตรง ซึ่งตรวจ `ADMIN_TOKEN` แบบ fail-closed ผู้ดูแลพิมพ์ token ต่อหนึ่ง browser session และระบบเก็บไว้ใน `sessionStorage` เท่านั้น ปิดแท็บ/session แล้วต้องเข้าสู่ระบบใหม่

**ทำไมต้องเป็นโปรเจกต์แยก**: doGet/doPost เป็น entry point ระดับโปรเจกต์ การแยกไว้ทำให้ webhook ของโปรเจกต์หลัก (ต้อง anonymous ให้ LINE ยิงเข้า) กับ API ตั้งค่า (มี token คุมทุกคำขอ) ตั้งระดับ deployment อิสระกันได้ โปรเจกต์ webapp ยังไม่เก็บ LINE token; คำสั่งจัดการใบลาจะถูกส่งไป backend หลักที่ตรวจ `ADMIN_TOKEN` ซ้ำ เขียน audit และส่ง LINE เฉพาะตาม workflow ที่กำหนด ดังนั้น token นี้ถือเป็นสิทธิ์สำคัญและต้องหมุนทันทีหากสงสัยว่ารั่ว

ขั้นตอนที่ต้องทำผ่านหน้าเว็บ (ครั้งเดียว):

1. **ตั้ง Script Properties**: เปิดโปรเจกต์ Apps Script นี้ > **Project Settings > Script Properties > Add script property** แล้วเพิ่ม `ADMIN_TOKEN` เป็นข้อความสุ่มอย่างน้อย 32 ตัวอักษร (เช่นสร้างด้วย `openssl rand -hex 16`) โดยใช้ค่าเดียวกับโปรเจกต์หลัก, `SPREADSHEET_ID` เป็นรหัสจาก URL ของชีตหลักส่วนระหว่าง `/d/` กับ `/edit` และ `ALLOW_LEGACY_DIRECT=TRUE`; เก็บ token ไว้แจกเฉพาะผู้ดูแล
2. **ตรวจการตั้งค่า deployment**: **Deploy > Manage deployments > ดินสอ** ต้องเป็น **Execute as: Me** และ **Who has access: Anyone**
3. **ตั้งค่า URL ใน GitHub**: Settings > Environments > **liff** > เพิ่ม `ADMIN_API_URL` = URL `/exec` ของ deployment นี้
4. **ตั้งค่า Notion อ่านอย่างเดียว** (สำหรับหน้า "รายงาน"): สร้าง integration ที่ [notion.so/my-integrations](https://www.notion.so/my-integrations) ติ๊กเฉพาะ **Read content** แล้วใส่ token ใน Script Properties ชื่อ `NOTION_TOKEN_READONLY` + แชร์ database "ใบลา" ให้ integration ผ่าน **"..." > Connections**
5. เปิด `https://9gotstory.github.io/notion-notify/web/admin/` พิมพ์ token — ครบ 7 หน้า: ภาพรวม, บุคลากร, สิทธิ์วันลา, **จัดการใบลา** (เปลี่ยนผู้อนุมัติเฉพาะใบ+ปรับผลลาใช้จริง), วันหยุด, รายงาน และระบบ

**การจัดการ token**: เพิกถอน/เปลี่ยน token = แก้ค่า `ADMIN_TOKEN` ใน Script Properties (อ่านสดทุกคำขอ ทุกเครื่องที่ login ค้างไว้จะโดนเตะออกในคำขอถัดไปทันที) · เพิ่มผู้ดูแล = ส่ง token ให้เขา · ลิงก์ `/exec` เดิมที่เคยเป็นหน้าตั้งค่ารุ่นก่อน ตอนนี้เด้งไปหน้าใหม่อัตโนมัติ (bookmark เก่าไม่ตาย) · `ALLOWED_EDITORS` และการแชร์ชีตให้ editor รายคน**ไม่จำเป็นแล้ว** (สคริปต์รันเป็นเจ้าของโปรเจกต์ ไม่ใช่ผู้เข้าชม) ลบทิ้งได้

หน้านี้**ไม่มีปุ่มทดสอบส่งจริง** (เจตนา — ไม่เก็บ LINE token ไว้ที่โปรเจกต์นี้) ใช้เมนู "ทดสอบส่งตอนนี้" ในชีตตามข้อ 8 เหมือนเดิม ส่วนรายงานใช้ token ของ integration ที่อ่านได้อย่างเดียว แม้รั่วก็แก้/ส่งข้อความแทนใครไม่ได้

## 11. ระบบลางานเจ้าหน้าที่ (LIFF + ปุ่มอนุมัติบน LINE)

ต่อยอดจากข้อ 1-10 — เพิ่มการยื่นใบลาผ่านหน้าเว็บ LIFF, การ์ดขออนุมัติส่งตรงถึงผู้อนุมัติรายคน (แชท 1:1 กับ OA ไม่ผ่านกลุ่ม), ปุ่มอนุมัติ/ไม่อนุมัติบนการ์ด, และหัวข้อ "ผู้ลาวันนี้" ในข้อความสรุปเช้า

**ภาพรวมชิ้นส่วน** (ทำตามลำดับ 11.1 → 11.8 ใช้เวลารวม ~1 ชั่วโมง):

| ชิ้น | ทำหน้าที่ | อยู่ที่ไหน |
|---|---|---|
| ชีต `Staff` | ทำเนียบเจ้าหน้าที่ที่ผู้ดูแลเตรียมล่วงหน้า + workflow อนุมัติการผูก LINE | ในสเปรดชีตเดิม (สร้างด้วยเมนู) |
| ชีต `Approvers` | คอนฟิกผู้อนุมัติ: กลุ่มงานไหนใครอนุมัติ ส่งต่อ หัวหน้า สสอ. ไหม (+ เป็นรายการ dropdown กลุ่มงานของฟอร์ม) | ในสเปรดชีตเดิม (สร้างด้วยเมนู ผู้ดูแลกรอก) |
| Notion database "ใบลา" | เก็บใบลาทั้งหมด (source of truth) + audit | Notion workspace เดิม |
| `apps/main/Leave*.gs` | API รับคำขอจาก LIFF + ตรวจ token + อนุมัติผ่านปุ่ม + ยกเลิก/แก้ไขใบลา | โปรเจกต์ Apps Script เดิม (5 ไฟล์: Leave/LeaveCalc/LeaveApi/LeaveApproval/LeaveReports) |
| `web/liff-form/index.html` | หน้าฟอร์มยื่นลา + แท็บ "ของฉัน" (ยอดวันลา + รายการใบลาของตัวเอง แก้ไข/ยกเลิกได้เอง) | โฮสต์บน GitHub Pages |
| LINE Login channel + LIFF app | ให้หน้าฟอร์มรู้ว่าใครเปิด (LINE userId) | LINE Developers Console |

### 11.1 เตรียมชีตและคอนฟิกผู้อนุมัติ

1. เปิดสเปรดชีต > เมนู **ระบบแจ้งเตือนปฏิทิน > เตรียม/ตรวจสอบชีตทั้งหมด** — จะสร้างชีตที่ขาด (Settings/Holidays/Logs/**Approvers**/**Staff**) ใส่หัวตาราง บังคับรูปแบบวันที่เป็น `yyyy-MM-dd` เติมค่าตั้งต้นใน Settings เฉพาะ key ที่ยังไม่มี และตรวจความพร้อมของ ID, Script Properties, รายชื่อ, ผู้อนุมัติ, trigger และสวิตช์ระบบลา — รันซ้ำได้โดยไม่ทับข้อมูลเดิม และใช้ตรวจสุขภาพระบบเป็นระยะได้
2. **กรอกชีต `Approvers`** — หัวใจของการอนุมัติ กำหนดเองทั้งหมด ไม่มี hardcode:
   - **กลุ่มงาน** — ชื่อกลุ่มงานที่ใช้จับสายอนุมัติ
   - **ผู้อนุมัติ** — "ชื่อ สกุล" ของผู้อนุมัติของกลุ่มนั้น (หลายคนคั่นด้วยจุลภาค ใครกดปุ่มก่อนถือก่อน; ต้องเป็นชื่อที่ลงทะเบียนในระบบแล้ว)
   - **ส่งต่อให้ หัวหน้า สสอ.** — ใส่ `TRUE` ถ้าใบลาของกลุ่มงานนี้ต้องผ่าน หัวหน้า สสอ. อีกชั้น (เว้นว่าง = จบที่ผู้อนุมัติของกลุ่ม)
3. **ใส่รายชื่อ หัวหน้า สสอ.** ในแถว `second_approvers` ของชีต Settings — "ชื่อ สกุล" คั่นจุลภาค (หลายคนได้ ต้องลงทะเบียนในระบบแล้ว)
4. **เตรียมชีต `Staff` ล่วงหน้าจากทำเนียบที่เชื่อถือได้** อย่างน้อยคำนำหน้า, ชื่อ, สกุล, กลุ่มงาน, ตำแหน่ง, ประเภทบุคลากร, `รหัสบุคลากร` ที่ไม่ซ้ำ และ `สถานะบุคลากร=ACTIVE`; ห้ามให้ผู้ใช้กำหนดข้อมูลสิทธิ์เหล่านี้เอง
5. เมื่อผู้ใช้กรอกรหัสบุคลากร ระบบบันทึก `PENDING` และ LINE user ID ที่รออนุมัติ ผู้ดูแลเปิดหน้า **บุคลากร**, เทียบหลักฐาน แล้วกด **อนุมัติผูก** หรือ **ปฏิเสธ** พร้อมเหตุผล หลังอนุมัติเท่านั้นจึงยื่นลาได้
6. ระบบที่มี Staff เดิม: การรันเมนูเตรียมชีตครั้งแรกจะ grandfather บัญชี LINE เดิมเป็น `ACTIVE/APPROVED` เพื่อไม่ล็อกผู้ใช้ แต่ผู้ดูแลยังต้องเติมรหัสบุคลากรและตรวจทะเบียนย้อนหลัง

กฎที่ระบบใช้จากคอนฟิกนี้: ใบลา → ผู้อนุมัติของกลุ่มงาน → (ถ้ากลุ่มตั้งส่งต่อ) หัวหน้า สสอ. / ผู้ยื่นคือผู้อนุมัติของกลุ่มตัวเอง → ข้ามไป หัวหน้า สสอ. ทันที / **หัวหน้า สสอ. ยื่นเอง → นอกระบบ** (ยื่นช่องทางเดิม) / ผู้อนุมัติของกลุ่มยังไม่ลงทะเบียน → ใบลาขึ้น หัวหน้า สสอ. ทันที หรือเข้ากลุ่มหลักให้ผู้อนุมัติรายอื่นกดแทน

ระบบ snapshot ว่าใบใดต้องส่งต่อขั้นสองไว้ตอนยื่น/ส่งอนุมัติใหม่ การแก้ `Approvers.forward` ภายหลังมีผลเฉพาะใบใหม่ ไม่เปลี่ยนเส้นทางของใบที่กำลังรออยู่; หากต้องแก้เฉพาะใบให้ใช้ฟังก์ชันเปลี่ยนผู้อนุมัติพร้อมเหตุผล

> ย้ายระบบมาจากโครงเดิม? ถ้าชีต Staff เคยใช้โครง 7 คอลัมน์ที่มี "ระดับ" ให้สำรองข้อมูล แล้วแยกชื่อ/สกุลและย้ายเป็นโครง 9 คอลัมน์เดิมก่อนรันเมนู เมนูจะต่อคอลัมน์ identity ใหม่และ grandfather LINE ID เดิมเป็น `ACTIVE/APPROVED` หนึ่งครั้ง แต่ไม่สร้างรหัสบุคลากรให้เอง ส่วน `leave_types_needing_director` ใน Settings ลบได้ เพราะกฎส่งต่ออยู่ใน Approvers แล้ว

### 11.2 สร้าง Notion database "ใบลา"

สร้าง database ใหม่ (Full page database) ใน workspace เดิม ตั้งชื่อ property **ตรงตามนี้ทุกตัวอักษร** และ**ชนิดต้องถูก**:

| Property | ชนิด | options / หมายเหตุ |
|---|---|---|
| ผู้ลา | Title | เก็บชื่อเต็ม (คำนำหน้า+ชื่อ-สกุล) |
| กลุ่มงาน | Text | |
| ผู้ยื่น (ระบบ) | Text | ระบบเติม LINE userId — **อย่าแก้** ใช้ push ผลอนุมัติกลับ |
| ประเภทการลา | Select | ลาป่วย, ลากิจ, ลาพักร้อน, ลาคลอด, ลาอุปสมบท/ลาบวช, ลาช่วยเหลือภรรยาคลอดบุตร, อื่นๆ |
| วันที่ลา | Date | ใช้ช่วงวันที่ (End date) สำหรับลาหลายวัน |
| เหตุผล | Text | |
| สถานะ | Select | รอผู้อนุมัติ, รอหัวหน้า สสอ.อนุมัติ, อนุมัติ, ไม่อนุมัติ, ยกเลิก |
| ผู้อนุมัติปัจจุบัน | Text | ระบบเติม JSON ภายใน — **อย่าแก้** |
| บันทึกการอนุมัติ | Text | audit trail ระบบเติม |
| หมายเหตุระบบ | Text | ระบบเติม: ยอดใช้/สิทธิ์ของปี + คำเตือนตามระเบียบ (เกินสิทธิ์, ต้องมีใบรับรองแพทย์ ฯลฯ) — **เพิ่มภายหลังได้** ก่อนเพิ่มระบบจะไม่บันทึกฟิลด์นี้ก็ทำงานได้ |
| ช่วงวัน | Text | เต็มวัน (ค่าเริ่มต้น) / ครึ่งวันเช้า / ครึ่งวันบ่าย — **เพิ่มภายหลังได้** ก่อนเพิ่มระบบจะบันทึกเฉพาะใบเต็มวัน |
| ผู้ปฏิบัติงานแทน | Text | ระบบเก็บ JSON ของบุคลากรที่ลงทะเบียนแล้ว — เว้นว่างได้; ถ้ายังไม่มี property นี้ backend หลักจะเพิ่มให้อัตโนมัติเมื่อมีการเลือกครั้งแรก |
| จำนวนวันทำการ | Number | ระบบเติมเพื่อใช้กับตารางกำลังคน/รายงาน (ข้ามเสาร์-อาทิตย์และวันหยุดจากชีต Holidays) — ลาครึ่งวัน = 0.5; จำนวนวันใช้สิทธิ์ของลาคลอด/บวชคำนวณย้อนหลังจากช่วงวันที่ ไม่ต้องเพิ่ม property |
| Request ID | Text | ระบบเติม UUID เพื่อกันการส่งซ้ำ — **ห้ามแก้** |
| สถานะการแจ้ง | Select | รอแจ้ง, แจ้งแล้ว, แจ้งไม่สำเร็จ, ต้องตรวจสอบ |

จากนั้น: กด **"..."** > **Connections** > เพิ่ม integration เดิมที่ใช้กับปฏิทินงาน > คัดลอก Database ID จาก URL ไปวางในแถว `leave_database_id` ของชีต Settings

> ระบบเดิมที่มี option `ลาอุปสมบถ/ลาบวช` หรือ `ลาช่วยเหลือภริยาคลอดบุตร` ให้ **rename option เดิมใน Notion** เป็น `ลาอุปสมบท/ลาบวช` หรือ `ลาช่วยเหลือภรรยาคลอดบุตร` ตามลำดับ (อย่าลบแล้วสร้างใหม่) เพื่อให้ใบย้อนหลังเปลี่ยนตาม option เดิมโดยไม่สูญข้อมูล เมนูเตรียมชีตจะย้ายคำสะกดใน Settings/QuotaProfiles/LeaveBalances ให้อัตโนมัติ และ runtime ยังอ่านคำสะกดเดิมได้ระหว่าง migration

**หลักการคำนวณวันลาตามระเบียบสำนักนายกฯ ว่าด้วยการลาของข้าราชการ** (สรุปสิ่งที่ระบบทำอัตโนมัติ):

- **แยกหน่วยให้ถูกต้อง** — ตารางกำลังคนและรายงานนับวันทำการ (ข้ามเสาร์-อาทิตย์/วันหยุด) แต่ลาคลอดและลาอุปสมบท/ลาบวชตัดสิทธิ์เป็นวันปฏิทินต่อเนื่องจากวันที่เริ่ม–สิ้นสุด
- **ลาครึ่งวัน** — ป่วย/กิจ/พักผ่อน เลือกได้เมื่อลา 1 วัน (ครึ่งวันเช้า/บ่าย) นับเป็น 0.5 วันทำการ
- **ใช้ปีงบประมาณไทย** — 1 ตุลาคมถึง 30 กันยายน เรียกปีตามปีที่สิ้นสุด เช่น 1 ต.ค. 2569–30 ก.ย. 2570 = ปีงบประมาณ 2570 ระบบบล็อกใบลาคร่อม 30 ก.ย./1 ต.ค. และให้แยกยื่น แต่ใบคร่อม 31 ธ.ค./1 ม.ค. อยู่ใบเดียวได้
- **ตรวจสิทธิ์ต่อปีงบประมาณจากใบลาจริงใน Notion** — กิจ/พักผ่อน/ช่วยเหลือภรรยาคลอดนับวันทำการ; คลอดและอุปสมบท/บวชนับวันปฏิทิน หรือตามโควตาประเภทบุคลากรของผู้ยื่น (ข้อ 11.12) — นับรวมใบที่กำลังรออนุมัติ
- **คำเตือนอัตโนมัติ (เตือนอย่างเดียว ไม่บล็อก)**: เกิน/ครบสิทธิ์ปีงบประมาณนี้ · ลาป่วยเกิน 3 วันทำการต้องมีใบรับรองแพทย์ (≥30 วัน ต้องมีทุกครั้ง) · พักผ่อนเกิน 10 วัน/ครั้ง (ต้องเป็นสิทธิ์สะสม ≤45) · ในช่วงลามีงานที่ผู้ยื่นเป็นผู้รับผิดชอบ (เทียบชื่อจริงกับปฏิทินงาน — แนะนำให้ประสานมอบหมายคนไปแทน) · ลากิจล่วงหน้าไม่ถึง 3 วันทำการ — คำเตือนแสดงบนฟอร์ม บันทึกลงใบลา และแสดงในการ์ดให้ผู้อนุมัติเห็น แต่ระบบตัดข้อความลากิจล่วงหน้าออกเฉพาะเมื่อส่งการ์ดเข้า LINE กลุ่ม เพื่อให้ข้อความกลุ่มกระชับ
- **รายการประเภทการลาแก้ได้** ที่คีย์ `leave_type_options` ในชีต Settings — ชื่อที่ตรงตามระเบียบได้การตรวจสิทธิ์อัตโนมัติ ชื่ออื่นยื่นได้แต่ไม่มีการตรวจ

### 11.3 สร้าง LINE Login channel + LIFF app

**สำคัญ: ต้องอยู่ใน provider เดียวกับ Messaging API channel เดิม** เพราะ userId ของคนเดียวกันจะตรงกันทั้งสอง channel เฉพาะใน provider เดียวกันเท่านั้น (ไม่งั้น push ผลอนุมัติกลับหาผู้ยื่นไม่ได้)

1. LINE Developers Console > provider เดิม > **Create a new channel** > เลือกประเภท **LINE Login**
2. เข้า channel > แท็บ **LIFF** > **Add** ตั้งค่าดังนี้:
   - **Endpoint URL**: URL ของหน้าฟอร์มจากข้อ 11.5 (ต้องมี `/` ปิดท้าย เช่น `https://9gotstory.github.io/notion-notify/web/liff-form/` — ถ้าไม่มี slash GitHub จะ redirect ทำให้ URL ไม่ตรงกับที่ลงทะเบียนและ `liff.init()` พัง)
   - **Size**: Full screen
   - **Scopes**: `profile`, `openid` (ไม่ต้องติ๊ง `chat_message.write`)
   - **Bot link feature**: **Aggressive** — คนที่ยังไม่ได้แอด OA จะเจอหน้าชวนแอดอัตโนมัติตอนเปิดฟอร์ม (จำเป็น เพราะต้องแอดบอทไว้ถึงจะรับ push ผลอนุมัติได้)
   - **Module mode**: ปิด
3. จดค่า 2 อย่าง: **LIFF ID** (เริ่มด้วย `16...`) และ **Channel ID** ของ LINE Login channel (แท็บ Basic)

### 11.4 ตั้งค่าฝั่ง Apps Script

1. Project Settings > Script Properties > เพิ่ม `LOGIN_CHANNEL_ID` = Channel ID จากข้อ 11.3 (ใช้ตรวจว่า access token ที่ส่งมาออกจาก LIFF ของเราจริง กัน token จากแอปอื่น)
2. อัปเดตโค้ดทั้งหมดจาก `apps/main/` ใน repository นี้ (ง่ายสุด: `scripts/push-main.sh` — ไฟล์ระบบลาคือ Leave/LeaveCalc/LeaveApi/LeaveApproval/LeaveReports)
3. **Deploy > Manage deployments > แก้ไข deployment เดิม (ไอคอนดินสอ) > Version: New version > Deploy** — URL เดิมไม่เปลี่ยน
4. ยืนยันว่า `ALLOW_LEGACY_DIRECT=TRUE` เพื่อให้ LIFF และตารางงานเรียก deployment โดยตรง; ค่านี้ไม่เปิด LINE webhook ที่ตรวจลายเซ็นไม่ได้
5. สำหรับ LINE webhook ใช้ gateway และตั้ง `GATEWAY_SHARED_SECRET` เป็นค่าแนะนำ หากจำเป็นต้องใช้ URL Apps Script ตรงชั่วคราว ให้ตั้ง `ALLOW_UNSIGNED_LINE_WEBHOOK=TRUE` โดยบันทึกผู้อนุมัติความเสี่ยงและวันยกเลิกค่า

### 11.5 โฮสต์หน้าฟอร์มบน GitHub Pages (ค่าตั้งค่าอยู่ใน GitHub Environment)

**ห้ามโฮสต์หน้า LIFF บน Apps Script Web App** — exec URL จะ redirect ไป `*.googleusercontent.com` ทำให้ URL ไม่ตรงกับ endpoint ที่ลงทะเบียน และ `liff.init()` พังทุกครั้ง

หน้าฟอร์มมีเพียง placeholder และ GitHub Actions จะแทนด้วย LIFF ID กับ Apps Script URL ก่อน deploy ไม่มี token ฝังในไฟล์

**ตั้งค่าครั้งเดียว ตามลำดับนี้:**

1. ถ้า repository นี้เป็น **private** ต้องเลือกก่อน: เปิดเป็น public (ไฟล์ใน repo ไม่มี secret ใดๆ — token ทั้งหมดอยู่ใน Script Properties / Environment เท่านั้น) หรือสร้าง repository สาธารณะใหม่แยกให้ฟอร์มโดยเฉพาะ
2. **Settings > Environments > New environment** ตั้งชื่อ `liff` แล้วเพิ่มค่า:
   - `LIFF_ID` = จากข้อ 11.3
   - `API_URL` = URL `/exec` ของ Apps Script หลัก
   - `ADMIN_API_URL` = URL `/exec` ของ Apps Script webapp
   - `SCHEDULE_LIFF_ID` = LIFF ID สำหรับหน้าตาราง (ไม่บังคับ ถ้ายังใช้โหมดสาธารณะอย่างเดียว)
3. **Settings > Pages > Source: "GitHub Actions"** (สำคัญ — ถ้ายังเป็น "Deploy from a branch" ให้เปลี่ยน ไม่งั้น workflow deploy ไม่ได้ และหน้าที่ถูกเสิร์ฟจะเป็นไฟล์ placeholder ที่ยังไม่ถูกแทนที่ ฟอร์มจะขึ้นว่า "ระบบยังไม่พร้อมใช้งาน")
4. `git push` — Actions จะรันเอง (ดูได้ที่แท็บ Actions) หน้าฟอร์มอยู่ที่ `https://<username>.github.io/notion-notify/web/liff-form/` และเสิร์ฟเฉพาะหน้าเว็บ ไม่โฮสต์ไฟล์ .gs ของ repo

**แก้ค่าภายหลัง** (เช่น เปลี่ยน deployment ของ Apps Script): แก้ใน Settings > Environments > liff แล้วไปแท็บ Actions > Deploy LIFF form > Run workflow — ไม่ต้องแก้โค้ดและไม่ต้อง commit

**ทดสอบในเครื่อง**: copy `web/liff-form/index.html` ไปไฟล์อื่น แทนที่ placeholder ด้วยค่าจริงแล้วเปิดผ่าน local server ก็ได้ (อย่า commit ไฟล์ที่ใส่ค่าจริงเด็ดขาด)

**การแก้สไตล์หน้าฟอร์ม**: หน้าใช้ **Tailwind v4** — คลาสอยู่ใน `index.html` ตามปกติ ส่วนสี/ฟอนต์ของธีมประกาศที่ `web/liff-form/src/styles.css` ผ่าน `@theme` (ธรรมเนียม CSS-first ของ v4) **ไม่ต้อง build เองตอนแก้** — push แล้ว GitHub Actions คอมไพล์ CSS ให้อัตโนมัติทุกครั้ง (ผลลัพธ์เป็น `styles.css` same-origin กับหน้า ไม่พึ่ง CDN ภายนอก เพราะเครือข่ายองค์กรบางแห่ง reset การเชื่อมต่อไป `cdn.tailwindcss.com`) ถ้าอยากดูผลในเครื่องก่อน:

```bash
cd web/liff-form
npm install        # ครั้งแรกเท่านั้น
npm run build:css  # สร้าง styles.css สำหรับเปิดผ่าน local server (ไฟล์นี้ถูก gitignore)
```

### 11.6 ปุ่ม "ยื่นลา" บน OA (Rich Menu)

ไปที่ [LINE Official Account Manager](https://manager.line.biz/) > channel ของ OA > **Rich Menu** สร้างเมนูใหม่: พื้นที่แตะเต็มความกว้าง 1 ปุ่ม ข้อความ "ยื่นลา 🗓️" Action เลือก **ลิงก์** แล้ววาง URL ของ LIFF (`https://liff.line.me/<LIFF_ID>`) จากนั้นตั้งเป็น default rich menu — ทำผ่านหน้าเว็บทั้งหมด ไม่ต้องเขียนโค้ด (จะแชร์ลิงก์ปักหมุดในกลุ่มแทนก็ได้ ใช้งานได้เหมือนกัน)

### 11.7 การใช้งานครั้งแรกของแต่ละคน

เจ้าหน้าที่แตะปุ่ม "ยื่นลา" > เพิ่ม OA เป็นเพื่อนเมื่อระบบแสดงคำเชิญ > กรอก **รหัสบุคลากร** > ส่งคำขอผูกบัญชี > รอผู้ดูแลอนุมัติ เมื่ออนุมัติแล้วจึงเปิดฟอร์มยื่นลาได้ ข้อมูลชื่อ กลุ่มงาน ตำแหน่ง และประเภทบุคลากรมาจากทำเนียบ Staff เท่านั้น

**บทบาทผู้ดูแล**: ตรวจรายการ `PENDING` ในหน้า **บุคลากร** เทียบรหัสบุคลากรกับแหล่งข้อมูล HR และยืนยันเจ้าของบัญชี LINE ผ่านช่องทางองค์กร ระบุเหตุผล/หลักฐานทุกครั้ง ห้ามอนุมัติจาก display name เพียงอย่างเดียว

### 11.8 ทดสอบและบำรุงรักษา

- **เปิด/ปิดระบบลา**: กดเมนู **"เปิด/ปิดระบบลา"** เพื่อสลับสถานะ (หรือแก้คีย์ `leave_system_enabled` ในชีต Settings เป็น `FALSE`/`TRUE` เองก็ได้) — เมื่อปิด: การลงทะเบียนใหม่และการยื่นใบลาถูกปฏิเสธพร้อมข้อความแจ้ง (แก้ข้อความได้ที่คีย์ `leave_closed_message` เช่นระบุช่วงเวลาปิด+ผู้ติดต่อ) ส่วน**ใบลาเดิมที่รออนุมัติยังกดปุ่มได้ตามปกติ** และสรุปเช้ายังแสดงผู้ลาที่อนุมัติแล้ว — ทุกการสลับถูกบันทึกในชีต Logs
- **เปิด/ปิดการอนุมัติใบลา (โหมดแจ้งลาอัตโนมัติ)**: กดเมนู **"เปิด/ปิดการอนุมัติใบลา"** (หรือแก้คีย์ `leave_approval_enabled` เป็น `FALSE`) — เมื่อปิด: การยื่นลา**บันทึกเป็น "อนุมัติ" ทันที**โดยไม่ต้องมีใครกดปุ่ม และไม่ต้องมีคอนฟิกผู้อนุมัติในชีต Approvers เลย ระบบแจ้งการ์ด (ไม่มีปุ่ม) เข้ากลุ่มหลัก + ยืนยันกลับผู้ยื่น และใบลาขึ้นสรุปเช้า "ผู้ลาวันนี้" ทันที — เหมาะช่วงที่ไม่ต้องการกระบวนการอนุมัติ ใบลาเดิมที่รออนุมัติอยู่ยังกดปุ่มได้จนจบ
- **สรุปวันลารายเดือน**: วันทำการแรกของแต่ละเดือน ระบบแนบสรุปใบลาที่อนุมัติของเดือนก่อนหน้าท้ายข้อความสรุปเช้า (รายคน×ประเภท + รวมทั้งเดือน) — รวมอยู่ในข้อความเดียวกัน จึงไม่ใช้โควตา LINE เพิ่มแม้แต่ข้อความเดียว ปิดได้ที่คีย์ `monthly_leave_summary_enabled` (FALSE = ปิด) เดือนที่ไม่มีใครลาเลยจะไม่มีส่วนสรุป (ระบบจำไว้ใน Script Properties คีย์ `last_monthly_leave_summary` ว่าเดือนไหนจัดไปแล้ว) / ทดสอบหน้าตาได้ด้วยเมนู **"ทดสอบสรุปรายเดือน"** โดยไม่ต้องรอข้ามเดือน
- **ใบลาค้างอนุมัติ**: trigger ตรวจทุกชั่วโมง — ครบ 24 ชั่วโมงเตือนผู้อนุมัติปัจจุบัน; ครบ 48 ชั่วโมงแจ้ง หัวหน้า สสอ. เพื่อทราบแต่ไม่เปลี่ยนผู้อนุมัติอัตโนมัติ; ปรับชั่วโมงได้ที่ `leave_pending_reminder_hours` / `leave_pending_escalation_hours` ใน Settings
- **ผู้อนุมัติสำรองเฉพาะใบ**: Admin ใช้หน้า **จัดการใบลา**; หัวหน้า สสอ. ใช้แท็บ **อนุมัติ** ใน LIFF เลือกได้จากบุคลากรที่ลงทะเบียนแล้วทั้งหมด มีผลเฉพาะใบนั้น ต้องระบุเหตุผล ระบบแจ้งผู้อนุมัติเดิม/ใหม่/ผู้ยื่น และปิดการ์ดเก่าทันที
- **ผลการลาใช้จริง**: Admin ปรับได้เฉพาะใบอนุมัติ/ยกเลิก เช่น ลาจริงครึ่งวันแทนเต็มวัน; ระบบตรวจทับซ้อน คำนวณยอดใหม่ บันทึก audit และแจ้งผู้เกี่ยวข้องแบบ 1:1
- **ทดสอบหน้าตาการ์ด**: เมนู "ทดสอบการ์ดใบลา" ส่งการ์ดตัวอย่างเข้ากลุ่มหลัก (ปุ่มบนการ์ดตัวอย่างกดแล้วไม่มีผล)
- **ทดสอบ E2E**: ยื่นลาจริงด้วยบัญชีทดสอบ 1 ใบ > ตรวจสอบว่าการ์ดถึงผู้อนุมัติแบบ 1:1 > กดปุ่ม > ตรวจสอบสถานะและบันทึกการอนุมัติใน Notion > ตรวจสอบว่าผู้ยื่นได้รับผล > เช้าวันทำการถัดไปต้องมี "ผู้ลาวันนี้" ในการ์ดปฏิทิน
- **เปลี่ยนบัญชี LINE / กรอกข้อมูลผิด**: ผู้ดูแลลบแถวนั้นในชีต Staff (หรือล้างคอลัมน์ LINE User ID) แล้วให้เจ้าของเปิดฟอร์มลงทะเบียนใหม่
- **ยกเลิก/แก้ไขใบลาที่ยื่นไปแล้ว**: ผู้ยื่นทำเองได้ในแท็บ **"ของฉัน"** ของฟอร์มลา (ดูหัวข้อ 11.10) — **ยกเลิก**ได้ทั้งใบรออนุมัติและใบอนุมัติแล้วก่อนวันเริ่มลา (ระบบแจ้งผู้อนุมัติ 1:1 อัตโนมัติ และคืนยอดสิทธิ์ทันที) / **แก้ไข**ได้เฉพาะใบที่ยังรออนุมัติ (แก้แล้วระบบคำนวณวันทำการใหม่ ส่งการ์ดขออนุมัติใหม่ และบันทึกการแก้ไขลง audit) — ใบที่เริ่มลาแล้วหรือใบอนุมัติแล้วที่ต้องแก้วันที่ ให้ติดต่อผู้ดูแล
- **Unit tests**: เมนู "รัน Unit Tests" ครอบคลุมตัวสร้าง payload/การ์ด/กฎวันทำการ/บันไดผู้อนุมัติ/กติกาแก้ไข-ยกเลิก/สรุปรายเดือน ทั้งหมด

### 11.9 หน้าดูตารางงาน (`/web/schedule/`)

หน้าเว็บแยกจากฟอร์มลา ดูปฏิทินการปฏิบัติงานย้อนหลัง 1 เดือน–ล่วงหน้า 6 เดือน แบบ **สองโหมด**: สาธารณะ (มีลิงก์ก็ดูได้ — เห็นงาน/เวลา/สถานที่) และเจ้าหน้าที่ (เข้าสู่ระบบ LINE + ลงทะเบียนในชีต Staff แล้ว — เห็น**ผู้รับผิดชอบ/รายละเอียด/หมายเหตุ/ผู้ลา**เพิ่ม) รายการที่แสดงใช้ whitelist สถานะเดียวกับข้อความเช้า (เฉพาะ "ยืนยันแล้ว")

**ผู้ลาในหน้าตารางงาน**: มุมมองเจ้าหน้าที่แสดงการ์ด "🏖️ ลา" (โทนอำพัน) ของใบลา**ที่อนุมัติแล้ว**ที่คร่อมวันนั้น — แสดงเฉพาะ**วันทำการ** (ข้ามเสาร์-อาทิตย์และวันหยุดราชการ ตามวิธีนับวันทำการของใบลา วันหยุดจึงไม่มีหัวข้อวันโผล่มาเพราะมีแต่คนลา) พร้อมชื่อจริง + ประเภท (+ ช่วงวันของใบหลายวัน / ครึ่งวัน) พร้อม**ตรวจชนกันอัตโนมัติ**: งานที่มีผู้รับผิดชอบ (หรือ "ทุกคน") กำลังลาในวันนั้นจะขึ้นแถบเตือน ⚠ สีอำพันบนการ์ดงาน โดยยึดหลักเดียวกับสรุปเช้า: ใบที่ยังรออนุมัติไม่เปิดเผย และชื่อบุคลากรเป็นข้อมูลภายในจึงไม่ส่งให้โหมดสาธารณะ (ถ้าอยากเปิดให้สาธารณะเห็นด้วย แก้ที่ `apiSchedule_` ใน `apps/main/Calendar.gs` — ย้ายบล็อกดึง `leaves` ออกจากเงื่อนไข `if (full)`)

**ใช้งานได้ทันทีแบบสาธารณะ** หลัง push (ไม่ต้องตั้งค่าอะไรเพิ่ม) ที่ `https://<user>.github.io/notion-notify/web/schedule/` — ส่วนปุ่มล็อกอินเจ้าหน้าที่ต้องสร้าง **LIFF app ตัวที่สอง** (LIFF ผูกกับ URL เดียวต่อแอป ฟอร์มลาใช้ URL ของตัวเองไปแล้ว):

1. LINE Developers Console > provider เดิม > **LINE Login channel เดิม** > tab LIFF > Add: Endpoint = `https://<user>.github.io/notion-notify/web/schedule/` (**ปิดท้ายด้วย `/`**), Size **Full**, Bot link = **Off หรือ Normal** (หน้านี้ไม่จำเป็นต้องบังคับแอดเพื่อน — ต่างจากฟอร์มลา)
2. คัดลอก LIFF ID ใหม่ ไปใส่ GitHub Environment "liff" เป็น **`SCHEDULE_LIFF_ID`** (secret หรือ variable ก็ได้)
3. push อะไรก็ได้ใน `web/schedule/` หรือ `web/liff-form/` หรือกด workflow ด้วยตัวเอง (Actions > Deploy LIFF form > Run workflow) แล้วทดสอบ: เปิดในแอป LINE ต้องขึ้นป้าย "✓ มุมมองเจ้าหน้าที่" / เปิดด้วย Chrome ต้องเป็น "มุมมองทั่วไป" พร้อมปุ่มล็อกอิน
4. เอาลิงก์ `https://liff.line.me/<SCHEDULE_LIFF_ID>` ไปทำปุ่มที่สองบน Rich Menu (เช่น "ตารางงาน 📅") หรือแชร์ URL ตรง `.io/notion-notify/web/schedule/` ปักหมุดในกลุ่มก็ได้

**หมายเหตุ**: ถ้าไม่ต้องการโหมดสาธารณะ (อยากบังคับล็อกอินก่อนเห็นข้อมูลเลย) หรืออยากซ่อนฟิลด์อื่นเพิ่ม แก้ที่ `toScheduleItem_` ใน `apps/main/Calendar.gs` จุดเดียว

### 11.10 แท็บ "ของฉัน" — ยอดวันลา + แก้ไข/ยกเลิกใบลาด้วยตัวเอง

ในหน้าฟอร์มลาเดิม (`web/liff-form/`) มี 2 แท็บสำหรับทุกคน และแท็บที่ 3 เฉพาะรายชื่อใน `second_approvers` — ไม่ต้องสร้าง LIFF app ใหม่:

- **แท็บ "ยื่นลา"** = ฟอร์มเดิมทุกอย่าง
- **แท็บ "ของฉัน"** มี 2 ส่วน:
  - **ยอดวันลาปีงบประมาณนี้** — ประเภทรายปีแสดงใช้ไป / สูงสุด / เหลือ; คลอด/บวช/ช่วยเหลือภรรยาคลอดแสดงยอดปี + เกณฑ์อ้างอิงและคำว่า "ตรวจรายกรณี" (คำนวณสดจากใบจริงและรวมใบรออนุมัติ แต่ไม่ตัดสินสิทธิ์ประเภทผูกเหตุการณ์แทน HR)
  - **ใบลาของฉันตั้งแต่ปีงบประมาณก่อนถึงปีถัดไป** — รายการทุกใบทุกสถานะพร้อมป้ายสี (รอ/อนุมัติ/ไม่อนุมัติ/ยกเลิก) เพื่อให้จัดการใบที่ยื่นย้อนหลังหรือล่วงหน้าได้ และมีปุ่มตามสถานะ:
    - **แก้ไข** (เฉพาะใบรออนุมัติ): เปิดฟอร์มเดิมพร้อมข้อมูลเดิมให้แก้ → บันทึกแล้วระบบคำนวณวันทำการ/คำเตือนใหม่ ตั้งสถานะกลับ "รอผู้อนุมัติ" (รันผู้อนุมัติใหม่จากชีต Approvers ล่าสุด) ส่งการ์ดใหม่ให้ผู้อนุมัติ และเติมบรรทัด audit "ผู้ยื่นแก้ไขใบลา" ลงใบเดิมใน Notion
    - **ยกเลิก** (ใบรออนุมัติ + ใบอนุมัติแล้วก่อนวันเริ่มลา): ไม่ต้องขออนุมัติยกเลิกซ้ำ — ใบรอถูกถอนจากผู้อนุมัติที่ค้าง (แจ้ง 1:1), ใบอนุมัติแล้วแจ้งผู้อนุมัติของกลุ่มงานปัจจุบัน 1:1 ให้ทราบ, ปุ่มบนการ์ด LINE เก่าใช้ไม่ได้ทันที และยอดสิทธิ์คืนทันที / ใบที่เริ่มลาแล้วหรือผ่านไปแล้วยกเลิกเองไม่ได้ (กันแก้ประวัติย้อนหลัง — ติดต่อผู้ดูแลแทน)
- **แท็บ "อนุมัติ"** (เฉพาะ หัวหน้า สสอ.) แสดงใบรอทั้งหมด และเลือกผู้อนุมัติสำรองจากบุคลากรที่ลงทะเบียนแล้วทั้งหมด ต้องกรอกเหตุผลทุกครั้ง
- การแจ้งทั้งหมดเป็นแชทส่วนตัว 1:1 เท่านั้น ไม่มีอะไรเกี่ยวกับใบลาของแต่ละคนหลุดเข้ากลุ่มหลัก
- โหมดต่างๆ: ปิดระบบลา (`leave_system_enabled` = FALSE) ยังยกเลิกได้ (เหมือนปุ่มอนุมัติที่ยังใช้ได้) แต่แก้ไขไม่ได้เพราะการแก้ = การยื่นใหม่ / โหมดปิดการอนุมัติ: แก้ไขแล้วคงสถานะอนุมัติและแจ้งการ์ดเข้ากลุ่มหลักตามธรรมเนียมของโหมดนั้น

**รายงานสรุปรายเดือน/ปีงบประมาณ**: ดูได้ 2 ที่ — (1) สรุปย่อเดือนที่ผ่านมาแนบท้ายข้อความสรุปเช้าวันทำการแรกของแต่ละเดือน (ปิด/เปิดได้ที่ `monthly_leave_summary_enabled` ดูหัวข้อ 11.8) (2) ตารางละเอียดรายคน×ประเภท เลือกปีงบประมาณ/เดือน + ดาวน์โหลด CSV ที่หน้า **"รายงาน"** ของหน้าผู้ดูแล (ตั้งค่า Notion อ่านอย่างเดียวครั้งเดียวตามข้อ 10 ข้อ 4)

### 11.11 สมุดรายการปรับยอดวันลา (LeaveBalances)

ชีต `LeaveBalances` (สร้างอัตโนมัติด้วยเมนู "เตรียม/ตรวจสอบชีตทั้งหมด") เก็บ "รายการปรับ" ยอดวันลารายคนต่อปีงบประมาณ — สำหรับเรื่องที่ระบบคำนวณเองไม่ได้:

| คอลัมน์ | ความหมาย | ตัวอย่าง |
|---|---|---|
| ปีงบประมาณ (พ.ศ.) | ปีงบประมาณที่รายการมีผล | 2570 |
| ชื่อ สกุล | ตรงกับทำเนียบ Staff | ธนกร ใจดี |
| ประเภทการลา | ชื่อประเภทตาม Settings | ลาพักร้อน |
| **ยกมา** (วันใช้สิทธิ์) | **เพิ่มเข้าสิทธิ์**ของปีนั้น | พักร้อนสะสมจากปีก่อน 5 |
| **ใช้เพิ่ม** (วันใช้สิทธิ์) | **เพิ่มเข้ายอดใช้ไป** | ลาก่อนเริ่มใช้ระบบ 4 |
| เหตุผล / บันทึกเมื่อ | เหตุผลอย่างน้อย 5 ตัวอักษร + เวลา | (ระบบเติมเวลาให้) |
| Request ID | รหัสคำขอที่ระบบเติมเพื่อกัน retry แล้วสร้างรายการซ้ำ | UUID |
| ผู้ดำเนินการ | บทบาทผู้บันทึกรายการ | ผู้ดูแลระบบ |

**หลักการ**: ยอดที่แสดง **ทุกจุดของระบบ** = ผลรวมจากใบลาจริงใน Notion + รายการในสมุดนี้ — ใบลายังเป็นฐานหลักเสมอ (แก้ใบลาใน Notion ยอดก็ถูกตามทันที สมุดนี้ไม่เก็บยอดสะสมจึงไม่มีทางเพี้ยน) จุดที่รวมยอดแล้ว: การ์ด "ยอดวันลา" ในแท็บของฉัน (แถวที่มี `*` = มีการปรับ) · คำเตือนสิทธิ์และบรรทัดยอดบนการ์ดใบลาตอนยื่น/แก้ไข (ใช้สิทธิ์รวมยกมา — ไม่เตือนเกินจริง)

**เริ่มใช้กลางปีงบประมาณ**: ก่อนเปิดให้ยื่นจริง HR/Admin ต้องบันทึกยอดที่รับรอง ณ วันก่อนเปิดระบบ โดยใส่ `ยกมา` = สิทธิ์สะสมที่ติดมาจากปีก่อน และ `ใช้เพิ่ม` = ยอดที่ใช้ตั้งแต่ 1 ตุลาคมถึงวันก่อนเปิดระบบ แยกตามคน/ประเภทลา หากไม่มีข้อมูลเดิม ระบบจะถือว่าใช้เพิ่มเป็น 0 และไม่สามารถอนุมานย้อนหลังเองได้

**จัดการรายการ**: แท็บ **"ยอดวันลา"** บนหน้าเว็บตั้งค่า (เพิ่ม/แก้ไข/ลบ — ต้องระบุเหตุผลอย่างน้อย 5 ตัวอักษร; การเพิ่มมี Request ID กันบันทึกซ้ำ) เป็นช่องทางแนะนำ การแก้ชีตตรงต้องรักษาเหตุผลและ audit columns ให้ครบ แถวที่ปี/ตัวเลขไม่ถูกต้องจะถูกข้ามโดยไม่พังทั้งระบบ

**โควตาพื้นฐานตามสถานะ**: สิทธิ์ของแต่ละคนเริ่มจาก "ประเภทบุคลากร" ของคนนั้นก่อน (ดูหัวข้อ 11.12) แล้วค่อยบวกยกมา/ใช้เพิ่มจากสมุดนี้

**ปิดสิ้นปีงบประมาณ**: ก่อนสิ้นวันที่ 30 กันยายน รันเมนู **"ตรวจใบลาคร่อมปีงบประมาณ"** ก่อน หากพบใบเดิมต้องให้ HR แยกที่ 30 กันยายน/1 ตุลาคม จากนั้นใช้เมนู **"ร่างยอดยกมาปีถัดไป"** — ระบบจะไม่ยอมร่างหากยังพบใบคร่อมปี แล้วจึงคำนวณวันลาพักร้อนคงเหลือรายบุคคลและร่างรายการ "ยกมา" ให้อ่าน (จำกัดไม่เกินสิทธิ์รายปี 10 วันทำการตามระเบียบ) — **ตั้งใจไม่บันทึกอัตโนมัติ**: ผู้ดูแลตรวจตามระเบียบจริงของแต่ละคน แล้วนำไปบันทึกในปีงบประมาณถัดไปที่แท็บยอดวันลาเอง

### 11.12 โควตาสิทธิ์ตามประเภทบุคลากร (QuotaProfiles)

ระบบมี**ค่าเริ่มต้นเชิงปฏิบัติการครบ 6 ประเภทบุคลากร** (ข้าราชการ / พนักงานราชการ / พนักงานกระทรวงสาธารณสุข / ลูกจ้างประจำ / ลูกจ้างชั่วคราวรายเดือน / ลูกจ้างรายวัน) เพื่อช่วยเริ่มตั้งค่า แต่ไม่ใช่คำรับรองสิทธิทางกฎหมาย เพราะสิทธิจริงอาจต่างตามฉบับแก้ไข สถานะจ้าง สัญญา อายุงาน และนโยบายหน่วยงาน โดยเฉพาะประกาศพนักงานกระทรวงสาธารณสุขที่มีฉบับแก้ไขปี 2569

ค่าเริ่มต้นสำหรับผู้ที่**ไม่ระบุ**ประเภทบุคลากร: ลากิจ 45 · พักร้อน 10 วันทำการ/ปี; คลอด 90 · บวช 120 วันปฏิทิน และช่วยเหลือภรรยาคลอด 15 วันทำการเป็นเกณฑ์อ้างอิงรายเหตุการณ์/ตามสถานภาพ; ลาป่วยไม่จำกัด ทั้งหมดเป็นเพียงค่าเตือนให้ผู้อนุมัติตรวจ ไม่ใช่การตัดสินสิทธิอัตโนมัติ

**โครง 3 ชั้น**: สิทธิ์สุดท้ายของคนหนึ่งในปีงบประมาณหนึ่ง = โควตาตามประเภทบุคลากรของคนนั้น (ชีต `QuotaProfiles`) → ทับด้วยแถวระบุปีงบประมาณเฉพาะ (เช่น เข้ากลางปี ตั้งพักร้อน = 5) → บวกยอดยกมา/ใช้เพิ่มจากสมุด LeaveBalances (ข้อ 11.11) — มีผลที่แท็บของฉัน, คำเตือนตอนยื่น/แก้ไข, การ์ดอนุมัติ และเมนูร่างยอดยกมา ส่วนรายงานกำลังคนนับเฉพาะใบจริงใน Notion

| ชิ้น | ที่อยู่ | หมายเหตุ |
|---|---|---|
| ประเภทบุคลากรของแต่ละคน | คอลัมน์ I ของชีต Staff | ผู้ดูแลกำหนดจากทำเนียบที่รับรองก่อนอนุมัติการผูก LINE — ว่าง = ใช้ค่าเริ่มต้นระบบ |
| ตารางโควตา | ชีต `QuotaProfiles`: `ปีงบประมาณ (ว่าง = ทุกปี) | ประเภทบุคลากร | ประเภทการลา | เกณฑ์วันใช้สิทธิ์ | หมายเหตุ` | หน่วยของคลอด/บวชคือวันปฏิทิน ประเภทอื่นเป็นวันทำการ; กิจ/พักผ่อน/ป่วยใช้ยอดปีงบประมาณ ส่วนคลอด/บวช/ช่วยเหลือภรรยาคลอดเป็นเกณฑ์รายเหตุการณ์หรือสถานภาพที่ HR ต้องตรวจ; **0 = ไม่มีสิทธิ์ตามนโยบายที่ HR ยืนยันแล้วเท่านั้น** |
| จัดการ | หน้าเว็บตั้งค่า > แท็บ **"โควตา"** | เพิ่ม/แก้/ลบโควตา + ตารางสถานะรายคน (เลือกแล้วกดบันทึก) หรือแก้ชีตตรงๆ |
| รายการตัวเลือกสถานะ | Settings คีย์ `employment_type_options` | ตั้งค่าเริ่มต้น: ข้าราชการ, พนักงานราชการ, พนักงานกระทรวงสาธารณสุข, ลูกจ้างประจำ, ลูกจ้างชั่วคราวรายเดือน, ลูกจ้างรายวัน, อื่นๆ |
| วันที่ HR รับรองค่าที่ใช้ | Settings คีย์ `leave_policy_reviewed_at` | ใส่ `YYYY-MM-DD` หลัง HR ตรวจทุกแถวกับระเบียบ/ประกาศ/สัญญาจ้างล่าสุด; health check จะเตือนเมื่อว่าง รูปแบบผิด อยู่ในอนาคต หรือเกิน 1 ปี |

**เริ่มใช้**: รันเมนู "เตรียม/ตรวจสอบชีตทั้งหมด" หนึ่งครั้ง ระบบจะสร้าง QuotaProfiles และเติมค่าเริ่มต้น 36 แถวโดยไม่ทับแถวที่ผู้ดูแลแก้เอง จากนั้น HR ต้องตรวจทุกแถวกับเอกสารที่ใช้กับหน่วยงานจริง ปรับผ่านหน้า "สิทธิ์วันลา" และใส่ `leave_policy_reviewed_at` ก่อน production เมนูนี้จะย้ายเฉพาะค่า seed เก่าที่เป็น 0 พร้อมหมายเหตุเดิมซึ่งพิสูจน์ว่าไม่เคยแก้เอง สำหรับการลาช่วยเหลือภรรยาคลอดบุตรของพนักงานราชการ/ลูกจ้าง เป็น 15 วัน; แถวที่ผู้ดูแลเคยปรับจะไม่ถูกแตะ

## ข้อจำกัดที่ควรรู้

- **ความแม่นยำของเวลา**: ระบบสร้าง one-time trigger ตาม `notify_time` แล้วนัดวันถัดไปใหม่หลังทำงานเสร็จ Apps Script อาจเริ่มช้ากว่าเวลาที่นัดเล็กน้อยตามภาระของระบบ แต่จะไม่ส่งย้อนหลังทันทีเพียงเพราะติดตั้ง trigger หลังเวลาที่กำหนด
- **เมื่อเปลี่ยนเวลา**: ไม่ว่าจะแก้ผ่านหน้าผู้ดูแลหรือชีตโดยตรง ต้องกดเมนู **ติดตั้ง/อัปเดตเวลาส่งอัตโนมัติ** เพื่อยกเลิกนัดเดิมและสร้างนัดใหม่
- **Direct mode**: LIFF/ตารางงานเปิดได้ด้วย `ALLOW_LEGACY_DIRECT=TRUE` ตามความเสี่ยงที่ยอมรับ แต่ LINE webhook ถูกปฏิเสธโดยค่าเริ่มต้นเพราะ Apps Script อ่าน `X-Line-Signature` ไม่ได้ ให้ใช้ gateway; `ALLOW_UNSIGNED_LINE_WEBHOOK=TRUE` เป็นข้อยกเว้นชั่วคราวที่ต้องบันทึกผู้อนุมัติความเสี่ยงและเฝ้าระวัง
- **ระบบลางาน — ความเป็นส่วนตัว**: การ์ดขออนุมัติและผลการอนุมัติเป็นแชทส่วนตัวระหว่าง OA กับรายบุคคล สรุปเช้าแสดงเฉพาะใบลาที่**อนุมัติแล้ว** (ใบที่ยังรอพิจารณาไม่เปิดเผย) ยกเว้นกรณี fallback ที่ไม่มีผู้อนุมัติรายคน การ์ดจะเข้ากลุ่มหลัก
- **ระบบลางาน — คนนอก**: เปิดลิงก์ได้แต่ยื่นลาไม่ได้หากไม่มีรหัสบุคลากร `ACTIVE` ใน Staff และยังไม่ได้รับอนุมัติการผูก LINE ระบบกัน LINE เดียวผูกหลายคนและบุคลากรหนึ่งคนมีคำขอซ้อน
- **ระบบลางาน — หัวหน้า สสอ. ลาเอง**: ยังยื่นนอกระบบและระบบจะปฏิเสธตามเดิม แต่เมื่อหัวหน้าไม่สะดวกอนุมัติใบของบุคลากร Admin หรือหัวหน้าเลือกผู้อนุมัติสำรองจากบุคลากรที่ลงทะเบียนแล้วให้เฉพาะใบนั้นได้ โดยไม่แก้สายอนุมัติถาวร
- **ปุ่ม "ไม่อนุมัติ" ไม่มีช่องเหตุผล**: ผู้อนุมัติแจ้งผู้ยื่นตัวต่อตัว หรือจดหมายเหตุใน Notion เอง
- **ถ้าบอทถูกลบออกจากกลุ่ม**: เชิญกลับแล้วทำขั้นตอนที่ 7 ด้วยรหัสจับคู่ใหม่ ไม่ต้อง deploy เพื่อเปลี่ยน Group ID
- **API หน้าผู้ดูแล (ข้อ 10)**: การติดตั้งปัจจุบันใช้ direct mode ตามความเสี่ยงที่เจ้าของระบบยอมรับ หน้า Admin เก็บ `ADMIN_TOKEN` ใน `sessionStorage` และ backend หลักตรวจ token ค่าเดียวกันก่อนเปลี่ยนผู้อนุมัติหรือปรับผลลาใช้จริง token ไม่ถูกฝังใน repo; ถ้าสงสัยว่ารั่วให้หมุนทั้งสองโปรเจกต์ทันที และ production ระยะยาวควรเพิ่ม SSO/IAP เพื่อระบุตัวผู้ดูแลรายบุคคล
- **ยกเลิกแข่งกับกดอนุมัติ**: การยกเลิก/แก้ไขและการกดปุ่มอนุมัติใช้ lock ตัวเดียวกันเพื่อไม่ให้ทำพร้อมกันจนสถานะเพี้ยน แต่เหลือช่องเล็กๆ ระดับวินาที (ผู้อนุมัติกดปุ่มไปแล้วสักพักก่อนคำขอยกเลิกมาถึง) ซึ่งเป็นคลาสเดียวกับการกดอนุมัติซ้ำสองปุ่มพร้อมกันที่ระบบยอมรับอยู่แล้ว — ปุ่มบนการ์ดเก่าจะตอบ "ใบลานี้ดำเนินการไปแล้ว" เสมอ
- **สรุปรายเดือน/รายงานกำลังคนนับตามวันเริ่มของใบ**: ใบลาคร่อมเดือน (เช่น 30–2 ของเดือนถัดไป) นับจำนวนวันทำการทั้งหมดไว้ในเดือนที่เริ่ม ไม่หักแยกข้ามเดือน รายงานนี้เป็นภาพกำลังคน ไม่ใช่เครื่องวินิจฉัยสิทธิ์คลอด/บวชแบบรายเหตุการณ์
- **ผู้ใช้ที่เปิดหน้าฟอร์มลาค้างไว้ก่อนอัปเดตระบบ**: จะยังเห็นหน้าเดิมไม่มีแท็บ "ของฉัน" จนกว่าจะปิดแล้วเปิดลิงก์ใหม่ (หน้าเก่าเรียกแต่ API เดิมจึงใช้งานต่อได้ปลอดภัย ไม่ error)
- **Notion API และข้อมูลจำนวนมาก**: query ทุกจุดวน pagination และหยุดด้วย error แทนการคืนข้อมูลบางส่วนเมื่อเกินเพดาน 2,000 รายการต่อคำขอเชิงธุรกิจ ควรเฝ้าดู rate-limit/error ใน Logs เมื่อปริมาณข้อมูลโตขึ้น
