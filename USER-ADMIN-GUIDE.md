# คู่มือผู้ใช้งาน ผู้อนุมัติ และผู้ดูแลระบบ

เอกสารนี้อธิบายการใช้งานประจำวันของระบบแจ้งเตือนปฏิทินและระบบลางาน โดยไม่ต้องเข้าใจโค้ดหรือการ deploy หากกำลังติดตั้งระบบหรือเตรียมเปิด production ให้ใช้ [SETUP.md](SETUP.md) และ [PRODUCTION-HANDOFF.md](PRODUCTION-HANDOFF.md) ร่วมด้วย

## 1. บทบาทในระบบ

| บทบาท | หน้าที่หลัก | ช่องทางใช้งาน |
|---|---|---|
| ผู้ใช้งาน | ลงทะเบียน ยื่น แก้ไข ยกเลิก และตรวจใบลาของตน | LIFF ใน LINE |
| ผู้อนุมัติ | ตรวจรายละเอียดและกดอนุมัติ/ไม่อนุมัติ | ข้อความส่วนตัวจาก LINE OA |
| หัวหน้า สสอ. | รับทราบใบค้างเกิน 48 ชั่วโมง และเปลี่ยนผู้อนุมัติให้เฉพาะใบเมื่อจำเป็น | แท็บ **อนุมัติ** ใน LIFF |
| ผู้ดูแล | ดูสุขภาพระบบ จัดบุคลากร ผู้อนุมัติ วันหยุด สิทธิ์ และรายงาน | หน้า Admin และ Google Sheet |
| HR/เจ้าของนโยบาย | รับรองประเภทบุคลากร เกณฑ์สิทธิ์ และข้อยกเว้น | หน้า Admin, เอกสารอ้างอิง และบันทึกการอนุมัติ |
| ผู้ดูแลเทคนิค | deploy, จัดการ secret/gateway, monitoring, backup และ incident | GitHub, Apps Script, cloud runtime, LINE และ Notion |

หนึ่งคนอาจมีมากกว่าหนึ่งบทบาท แต่ไม่ควรให้ผู้ดูแลเทคนิคตัดสินสิทธิ์แทน HR หรือให้ HR จัดการ secret โดยไม่จำเป็น

## 2. สิ่งที่ควรรู้ก่อนใช้งาน

- ข้อมูลใบลาและการอนุมัติส่งเป็นข้อความส่วนตัว ไม่ควรส่ง screenshot ที่มีเหตุผลการลาไปยังกลุ่มสาธารณะ
- ระบบแสดงคำเตือนสิทธิ์เพื่อช่วยตรวจ ไม่ได้วินิจฉัยสิทธิ์ทางกฎหมายแทน HR
- ลาคลอดและลาอุปสมบท/ลาบวชนับสิทธิ์เป็น **วันปฏิทิน** ส่วนรายงานกำลังคนและการลาประเภทอื่นใช้ **วันทำการ**
- ใบลาที่คร่อมปีถูกปฏิเสธ ผู้ใช้ต้องแยกเป็นหนึ่งใบต่อปี
- ปุ่มหรือหน้าเว็บอาจถูกเปิดซ้ำได้ แต่ระบบใช้ Request ID ป้องกันการสร้างใบซ้ำ ผู้ใช้ควรตรวจแท็บ **ของฉัน** ก่อนยื่นใหม่เมื่ออินเทอร์เน็ตขัดข้อง
- ห้ามส่งรหัสผู้ดูแล, LINE token, Notion token, shared secret หรือ Apps Script deployment URL ทางแชททั่วไป

## 3. คู่มือผู้ใช้งาน

### 3.1 ลงทะเบียนครั้งแรก

1. เปิดเมนู **ยื่นลา** จาก Rich Menu หรือ LIFF link ของหน่วยงาน โดยเปิดผ่าน LINE รุ่นปัจจุบัน
2. รอให้ระบบตรวจการเข้าสู่ระบบ หาก LINE ขออนุญาต ให้ตรวจชื่อแอปและกดยอมรับ
3. กรอก **รหัสบุคลากร** ที่หน่วยงานกำหนด ข้อมูลชื่อ กลุ่มงาน ตำแหน่ง และประเภทบุคลากรจะมาจากทำเนียบที่ผู้ดูแลเตรียมไว้
4. กด **ส่งคำขอผูกบัญชี** เพียงครั้งเดียว ระบบจะแสดงสถานะรอผู้ดูแลตรวจสอบ ระหว่างนี้ยังยื่นลาไม่ได้
5. หลังผู้ดูแลแจ้งผล ให้ปิดแล้วเปิด LIFF ใหม่ จากนั้นเปิดแท็บ **ของฉัน** เพื่อตรวจชื่อและยอดสิทธิ์ หากไม่ถูกต้องให้หยุดยื่นใบลาและติดต่อผู้ดูแล

ข้อควรระวัง: ห้ามส่งรหัสบุคลากรหรือหลักฐานยืนยันตัวตนในกลุ่ม LINE ผู้ดูแลต้องอนุมัติการผูกผ่านขั้นตอนในหัวข้อ 6.2 ก่อนบัญชีจึงใช้งานระบบลาได้

### 3.2 ยื่นใบลา

1. เปิดแท็บ **ยื่นลา**
2. เลือกประเภทการลา
3. เลือกวันเริ่มและวันสิ้นสุด หากลาเพียงวันเดียวให้ใช้วันเดียวกัน
4. เลือกเต็มวันหรือครึ่งวันเมื่อหน้าจอเปิดตัวเลือกนั้น
5. เลือก **ผู้ปฏิบัติงานแทน** จากบุคลากรที่ลงทะเบียนแล้วเมื่อมีผู้รับช่วงงาน หากไม่จำเป็นให้เว้นว่าง
6. กรอกเหตุผลเท่าที่จำเป็น หลีกเลี่ยงรายละเอียดสุขภาพหรือข้อมูลส่วนบุคคลที่ไม่จำเป็นต่อการอนุมัติ
7. อ่านจำนวนวันและคำเตือนทั้งหมด เช่น งานที่ชนวันลา สิทธิ์ใกล้หมด หรือกรณีที่ HR ต้องตรวจเป็นรายเหตุการณ์
8. กด **ส่งใบลา** หนึ่งครั้ง ระหว่างระบบกำลังส่งอย่าปิดหน้า
9. เมื่อเห็นข้อความสำเร็จ ให้ไปแท็บ **ของฉัน** และตรวจสถานะใบล่าสุด

ผลที่คาดหวัง:

- สถานะเริ่มต้นเป็น `รออนุมัติ` เมื่อระบบอนุมัติเปิดอยู่
- ผู้อนุมัติได้รับการ์ดในแชทส่วนตัว
- ผู้ปฏิบัติงานแทนได้รับข้อความส่วนตัวเมื่อมีการยื่น แก้ไข ยกเลิก หรือมีผลอนุมัติที่เกี่ยวข้อง
- หากหน่วยงานปิดการอนุมัติตามนโยบาย ใบอาจเปลี่ยนตามโหมดที่ผู้ดูแลกำหนด
- หากส่งแล้วหน้าจอค้าง ให้เปิดแท็บ **ของฉัน** ก่อนกดส่งใหม่

### 3.3 ตรวจยอดและสถานะในแท็บ “ของฉัน”

แท็บนี้แสดงยอดปีงบประมาณปัจจุบัน (1 ต.ค.–30 ก.ย.) และใบลาตั้งแต่ปีงบประมาณก่อนถึงปีถัดไป สถานะที่พบบ่อย:

| สถานะ | ความหมาย | สิ่งที่ผู้ใช้ทำได้ |
|---|---|---|
| รออนุมัติ | ส่งให้ผู้อนุมัติแล้ว | แก้ไขหรือยกเลิกได้ |
| อนุมัติ | ผู้อนุมัติยืนยันแล้ว | ยกเลิกเองได้เฉพาะก่อนวันเริ่มลา |
| ไม่อนุมัติ | ผู้อนุมัติไม่อนุมัติ | ติดต่อผู้อนุมัติหากต้องการเหตุผล |
| ยกเลิก | ใบถูกยกเลิกแล้ว | ใช้อ้างอิงประวัติ ไม่ต้องยื่นซ้ำเว้นแต่ต้องการลาใหม่ |

ยอดที่มีเครื่องหมาย `*` หมายถึงมีรายการยกมาหรือใช้เพิ่มจากผู้ดูแล ยอดคลอด/บวชและรายการที่ขึ้นว่า “ตรวจรายกรณี” ต้องให้ HR ตรวจสถานภาพหรือเหตุการณ์ ไม่ควรอ่านเป็นยอดคงเหลือรายปีอัตโนมัติ

### 3.4 แก้ไขใบลา

1. ไปแท็บ **ของฉัน** และเลือก **แก้ไข** ที่ใบสถานะรออนุมัติ
2. ตรวจว่าหน้าฟอร์มแสดงข้อความ “กำลังแก้ไขใบลา”
3. แก้ประเภท วันที่ ช่วงเวลา ผู้ปฏิบัติงานแทน หรือเหตุผล แล้วอ่านจำนวนวันใหม่
4. กด **บันทึกการแก้ไข**
5. ใบจะกลับไปรอการอนุมัติใหม่ ตรวจสถานะในแท็บ **ของฉัน** อีกครั้ง

ถ้าใบถูกผู้อนุมัติดำเนินการระหว่างที่เปิดหน้าแก้ไข ระบบจะไม่เขียนทับสถานะใหม่ ให้โหลดข้อมูลล่าสุดและติดต่อผู้อนุมัติเมื่อจำเป็น

### 3.5 ยกเลิกใบลา

1. ไปแท็บ **ของฉัน** และกด **ยกเลิก**
2. อ่านข้อความยืนยันและตรวจวันที่/ประเภทให้ถูกใบ
3. กดยืนยันแล้วรอจนรายการเปลี่ยนเป็น `ยกเลิก`

ใบที่เริ่มลาแล้วหรือเป็นประวัติย้อนหลังยกเลิกเองไม่ได้ ให้แจ้งผู้ดูแลพร้อม Request ID หรือ URL ของใบใน Notion โดยไม่ส่งข้อมูลลับ การยกเลิกใบอนุมัติแล้วก่อนวันเริ่มลาจะคืนยอดและแจ้งผู้อนุมัติที่เกี่ยวข้อง

### 3.6 ดูตารางงาน

1. เปิดหน้าตารางงานจากลิงก์ของหน่วยงาน
2. เลือกเดือนที่ต้องการ
3. ตรวจชื่อผู้รับผิดชอบ เวลา สถานที่ และสัญลักษณ์เตือนคนลาชนงาน
4. หากข้อมูลต่างจาก Notion ให้แจ้งผู้ดูแลปฏิทิน เพราะหน้าเว็บเป็นหน้าสรุป ไม่ใช่แหล่งแก้ข้อมูล

## 4. คู่มือผู้อนุมัติ

### 4.1 ก่อนอนุมัติใบแรก

- ลงทะเบียนบัญชี LINE ของตนในระบบให้ชื่อและกลุ่มงานตรงกับชีต `Staff`
- ให้ผู้ดูแลตรวจว่าชื่ออยู่ในสายอนุมัติของกลุ่มงานที่ถูกต้อง
- ทดสอบด้วยใบลาทดลองหนึ่งใบและยืนยันว่าการ์ดมาถึงแชทส่วนตัว ไม่ใช่กลุ่มหลัก

### 4.2 ตรวจและดำเนินการใบลา

1. เปิดการ์ดจาก LINE OA และตรวจชื่อ กลุ่มงาน ประเภท วันที่ จำนวนวัน และช่วงเวลา
2. อ่านคำเตือนเรื่องสิทธิ์ งานที่ชน และการตรวจรายกรณี
3. หากข้อมูลไม่ครบ ให้ติดต่อผู้ยื่นก่อนกดปุ่ม ผู้ยื่นสามารถแก้ใบที่ยังรออนุมัติได้
4. กด **อนุมัติ** หรือ **ไม่อนุมัติ** เพียงครั้งเดียว
5. ตรวจข้อความตอบกลับว่าระบบบันทึกผลสำเร็จ

หากปุ่มตอบว่าใบถูกดำเนินการแล้ว ให้เปิดข้อมูลล่าสุดหรือสอบถามผู้ยื่น ห้ามพยายามแก้สถานะด้วยการกดการ์ดเก่าซ้ำ ผู้อนุมัติไม่ควรใช้คำเตือนยอดสิทธิ์แทนคำวินิจฉัยของ HR

### 4.3 กรณีผู้อนุมัติไม่อยู่

ระบบเตือนผู้อนุมัติที่กำลังรับผิดชอบเมื่อใบค้างครบ 24 ชั่วโมง และแจ้งหัวหน้า สสอ. เมื่อครบ 48 ชั่วโมง โดยไม่เปลี่ยนผู้อนุมัติอัตโนมัติ

หัวหน้า สสอ. ที่อยู่ในรายชื่อ `second_approvers` สามารถจัดการได้ดังนี้:

1. เปิด LIFF แล้วเลือกแท็บ **อนุมัติ**
2. เลือกใบลาที่ค้าง เลือกผู้อนุมัติสำรองจากบุคลากรที่ลงทะเบียนแล้ว และระบุเหตุผล
3. ตรวจชื่อผู้ยื่นและผู้อนุมัติใหม่ แล้วกดยืนยันเพียงครั้งเดียว
4. ระบบจะแจ้งผู้อนุมัติเดิม ผู้อนุมัติใหม่ และผู้ยื่น พร้อมทำให้ปุ่มในการ์ดเดิมใช้ไม่ได้ทันที

การเปลี่ยนนี้มีผลเฉพาะใบนั้น ไม่แก้สายอนุมัติถาวรในหน้า **บุคลากร** และห้ามแชร์บัญชี LINE หรือส่งต่อ token หากผู้ที่ควรรับมอบหมายยังไม่ลงทะเบียน ให้ลงทะเบียนและตรวจตัวตนก่อนเลือก

## 5. คู่มือผู้ดูแลหน้า Admin

### 5.1 เข้าสู่ระบบอย่างปลอดภัย

1. เปิด URL หน้า Admin จาก bookmark ที่องค์กรรับรอง ไม่เปิดจากลิงก์ที่ส่งต่อโดยไม่ทราบที่มา
2. ตรวจว่าเป็น HTTPS และโดเมนถูกต้อง
3. กรอกรหัสผู้ดูแลที่ได้รับผ่านช่องทางปลอดภัย ระบบเก็บไว้ใน `sessionStorage` ของแท็บปัจจุบัน
4. เมื่อทำงานเสร็จ กด **ออกจากระบบ** และปิดแท็บ โดยเฉพาะเครื่องที่ใช้ร่วมกัน

ห้ามพิมพ์รหัสผู้ดูแลต่อท้าย URL ด้วยตนเอง หรือคัดลอก/แชร์ URL คำขอ, screenshot, issue, source code หรือ Google Sheet ที่อาจมีรหัส หากสงสัยว่ารั่วให้ผู้ดูแลเทคนิคหมุน `ADMIN_TOKEN` ทันที

### 5.2 หน้า “ภาพรวม” — ตรวจทุกวันทำการ

ตรวจรายการต่อไปนี้ก่อนแก้ข้อมูล:

- สถานะระบบแจ้งเตือนและ runtime health ต้องพร้อม
- เวลาส่งครั้งถัดไปต้องสอดคล้องกับ `notify_time`
- จำนวนบุคลากร ผู้อนุมัติ โควตา และวันหยุดไม่ลดลงผิดปกติ
- ไม่มีคำเตือนว่า HR ยังไม่ตรวจนโยบายหรือวันที่ตรวจเกินหนึ่งปี
- หาก health unavailable ให้หยุดการเปลี่ยนแปลงที่ไม่เร่งด่วนและตรวจ gateway/Apps Script ก่อน

### 5.3 หน้า “บุคลากร”

การแก้ตำแหน่งและประเภทบุคลากร:

1. ค้นหาชื่อบุคลากรและเทียบกับหลักฐานจาก HR
2. เลือกตำแหน่งจากรายการ `Settings.position_options` หรือเลือกประเภทบุคลากรจาก `Settings.employment_type_options`
3. ปุ่มบันทึกของช่องนั้นจะปรากฏเมื่อค่าเปลี่ยน กดบันทึกแล้วโหลดหน้าใหม่เพื่อตรวจผล
4. ตรวจหน้า **สิทธิ์วันลา** ว่ามี QuotaProfiles สำหรับประเภทนั้น

การแก้สายอนุมัติ:

1. ใช้ชื่อให้ตรงกับชีต `Staff` ทุกตัวอักษร
2. ระบุผู้อนุมัติอย่างน้อยหนึ่งคนต่อกลุ่มงาน และกำหนดลำดับตามนโยบายหน่วยงาน
3. บันทึกแล้วทดสอบด้วยใบลาทดลองของแต่ละกลุ่มงาน
4. เก็บผู้อนุมัติสำรองไว้ก่อนผู้อนุมัติหลักลาหรือพ้นหน้าที่

### 5.4 หน้า “สิทธิ์วันลา”

โควตาตามประเภทบุคลากร:

1. เลือกปีว่างสำหรับค่าที่ใช้ทุกปี หรือระบุปี พ.ศ. สำหรับข้อยกเว้นเฉพาะปี
2. เลือกประเภทบุคลากรและประเภทการลา
3. ใส่เกณฑ์วันใช้สิทธิ์ตามเอกสารที่ HR รับรอง ค่า `0` หมายถึงไม่มีสิทธิ์และต้องใช้เมื่อ HR ยืนยันแล้วเท่านั้น
4. ระบุเลขที่/วันที่เอกสารหรือเงื่อนไขสำคัญในหมายเหตุ โดยไม่ใส่ข้อมูลสุขภาพของบุคคล
5. บันทึกและตรวจว่าไม่มีแถวซ้ำที่มีปี ประเภทบุคลากร และประเภทการลาเดียวกัน

รายการปรับยอด:

- `ยกมา` เพิ่มสิทธิ์ เช่น วันพักร้อนสะสมจากปีก่อน
- `ใช้เพิ่ม` เพิ่มยอดใช้ไป เช่น ใบลาที่เกิดก่อนเริ่มใช้ระบบ
- ทุกการปรับต้องมีเหตุผล ผู้อนุมัติการปรับ และเอกสารอ้างอิงภายนอกระบบตามระเบียบองค์กร
- อย่าใช้รายการปรับเพื่อซ่อนหรือแทนที่ใบลาที่ควรมีอยู่ใน Notion

### 5.5 หน้า “จัดการใบลา”

ใช้หน้านี้เฉพาะเมื่อมีเหตุจำเป็นและมีหลักฐานประกอบ:

- **เปลี่ยนผู้อนุมัติเฉพาะใบ**: ใช้กับใบที่ยังรออนุมัติ เลือกบุคลากรที่ลงทะเบียนแล้วและระบุเหตุผล ระบบจะแจ้งผู้อนุมัติเดิม/ใหม่และผู้ยื่น พร้อมบันทึก audit
- **ปรับผลการลาใช้จริง**: ใช้กับใบที่อนุมัติหรือยกเลิกแล้ว เช่น วันกลับมาทำงานจริงต่างจากใบเดิม แก้สถานะ ประเภท ช่วงวัน ช่วงเวลา เหตุผล หรือผู้ปฏิบัติงานแทน แล้วระบุเหตุผลการปรับ ระบบจะตรวจวันทับซ้อนและคำนวณยอดใหม่
- อย่าใช้การปรับย้อนหลังแทนการแก้ข้อมูลทั่วไป หรือเปลี่ยนผลโดยไม่มีคำสั่ง/หลักฐานจากผู้มีอำนาจ
- หลังบันทึก ให้โหลดใบและรายงานซ้ำเพื่อตรวจสถานะ จำนวนวัน ยอดสิทธิ์ และผู้ปฏิบัติงานแทน

### 5.6 หน้า “วันหยุด”

1. ต้นปีให้เทียบรายการกับประกาศทางการ
2. เพิ่มวันหยุดด้วยวันที่ ชื่อ และประเภทที่ถูกต้อง
3. เมื่อมีวันหยุดพิเศษกลางปี ให้เพิ่มก่อนคำนวณหรืออนุมัติใบที่ได้รับผลกระทบ
4. ก่อนลบ ให้ตรวจใบลาในช่วงวันที่นั้น เพราะจำนวนวันทำการอาจเปลี่ยน
5. หลังแก้วันหยุด ให้ทดสอบใบลาตัวอย่างที่คร่อมวันหยุดหนึ่งรายการ

### 5.7 หน้า “รายงาน”

- เลือกปี/เดือนและกด **แสดง** ก่อนดาวน์โหลด CSV
- รายงานนับใบสถานะอนุมัติตามวันทำการและจัดใบคร่อมเดือนไว้ในเดือนที่เริ่ม
- รายงานใช้วางกำลังคน ไม่ใช่เอกสารรับรองสิทธิ์หรือยอดบัญชีเงินเดือน
- ก่อนส่งต่อไฟล์ ให้ตรวจผู้รับ สิทธิ์เข้าถึง และระยะเวลาเก็บข้อมูล

### 5.8 หน้า “ระบบ”

- เปลี่ยนเวลาส่งแล้วตรวจคำเตือนว่า trigger ได้รับการอัปเดต
- ตรวจ Group ID และ Database ID อย่างระมัดระวัง การเปลี่ยนค่าผิดอาจส่งข้อความผิดกลุ่มหรืออ่าน database ผิดชุด
- ใช้เมนูใน Google Sheet **ทดสอบส่งตอนนี้** สำหรับทดสอบ LINE เพราะหน้า Admin ไม่อ่าน LINE token
- อย่าใส่ token หรือ shared secret ลงช่อง Settings

## 6. งานประจำของผู้ดูแล

### 6.1 ทุกวันทำการ

- ตรวจหน้า **ภาพรวม**, ข้อผิดพลาดล่าสุด และเวลาส่งครั้งถัดไป
- ตรวจใบที่ `สถานะการแจ้ง = ต้องตรวจสอบ`
- ตรวจหน้า **จัดการใบลา** สำหรับใบค้างเกิน 48 ชั่วโมงและยืนยันว่ามีผู้รับผิดชอบ โดยไม่เปลี่ยนผู้อนุมัติหากยังไม่มีเหตุจำเป็น
- ตรวจคำขอผูก LINE สถานะ `PENDING` และการเปลี่ยนประเภทบุคลากร
- ตรวจว่าข้อความสรุปเช้าส่งถึงกลุ่มที่ถูกต้อง

### 6.2 เมื่อมีคำขอผูก LINE ใหม่

1. เปิดหน้า **บุคลากร** และตรวจรายการสถานะ `PENDING`; ระบบแสดงรหัสบุคลากร ข้อมูลจาก Staff และชื่อ LINE ที่ขอผูก
2. เทียบรหัสกับทำเนียบ HR และยืนยันเจ้าของบัญชีผ่านช่องทางองค์กร ห้ามใช้ LINE display name เพียงอย่างเดียว
3. หากตรง กด **อนุมัติผูก** และระบุหลักฐาน/เหตุผลอย่างน้อย 5 ตัวอักษร ระบบจึงย้าย LINE user ID เป็นบัญชีที่อนุมัติ
4. หากไม่ตรง กด **ปฏิเสธ** พร้อมเหตุผล ระบบจะล้าง candidate LINE ID โดยไม่ลบประวัติผู้ตรวจและเวลา
5. กรณีพนักงานพ้นสภาพให้เปลี่ยน `สถานะบุคลากร` เป็น `INACTIVE`; อย่าลบแถวเพราะต้องรักษา audit/history
6. กรณีเปลี่ยนบัญชี LINE ให้ revoke/ปฏิเสธบัญชีเดิมและยืนยันตัวตนใหม่ก่อนอนุมัติ ห้ามผูกสองบัญชีพร้อมกัน

### 6.3 ทุกสัปดาห์

- ตรวจ Apps Script Executions, gateway logs และ Logs sheet หา error ซ้ำ
- ตรวจหน้า **ระบบ > เก็บประวัติ Logs** ว่ายังคงเป็นค่าที่องค์กรอนุมัติ (ค่าเริ่มต้น 90 วัน) ระบบลบเฉพาะ `Logs` อัตโนมัติวันละครั้ง; `AuditLog` และ `SecurityEvents` ไม่อยู่ในนโยบายลบนี้
- ตรวจ `SecurityEvents` และ `AuditLog` เฉพาะผู้มีหน้าที่ โดยไม่ส่งออกทั้งชีตโดยไม่จำเป็น
- ตรวจ notification retry/dead-letter และแก้สาเหตุก่อน retry ด้วยมือ
- ทดสอบ health endpoint และหน้าหลักหนึ่งครั้ง

### 6.4 ทุกเดือน

- ตรวจรายงานกำลังคนกับข้อมูลใน Notion แบบสุ่มตัวอย่าง
- ตรวจสิทธิ์ของผู้เข้าใหม่ ย้ายกลุ่ม เปลี่ยนสถานะ หรือพ้นสภาพ
- ทบทวนผู้มีรหัส admin และผู้มีสิทธิ์เข้าถึง cloud/LINE/Notion
- ตรวจว่ามี backup ตามรอบและไม่มี secret อยู่ในไฟล์ backup ที่แชร์กว้างเกินไป

### 6.5 ก่อนปิดปีงบประมาณหรือเมื่อระเบียบเปลี่ยน

- HR ทบทวน `QuotaProfiles`, สัญญาจ้าง และข้อยกเว้นทุกประเภทบุคลากร
- อัปเดต `leave_policy_reviewed_at` หลังตรวจครบ ไม่ใช่เพียงเปลี่ยนวันที่
- ก่อนสิ้นวันที่ 30 กันยายน รันเมนู **ตรวจใบลาคร่อมปีงบประมาณ**; หากพบใบเดิมให้ HR แยกที่ 30 กันยายน/1 ตุลาคม แล้วจึงตรวจวันหยุดทางการ ร่างยอดยกมา และบันทึกยอดที่ HR รับรองในปีงบประมาณถัดไป
- ทดสอบการกู้คืนข้อมูลและหมุน secret ตามนโยบายองค์กร

## 7. ข้อผิดพลาดที่พบบ่อย

| อาการ | ผู้ใช้ควรทำ | ผู้ดูแลควรตรวจ |
|---|---|---|
| โหลดหน้า LINE ไม่สำเร็จ | เปลี่ยนเครือข่าย ปิดแล้วเปิด LIFF ใหม่ | GitHub Pages, LIFF ID, SDK, `API_URL` และ `ALLOW_LEGACY_DIRECT` |
| เซสชันหมดอายุ | รอระบบ login ใหม่หรือปิดหน้าแล้วเปิดใหม่ | LINE Login channel/LIFF configuration |
| ระบบตอบ `GATEWAY_REQUIRED` | หยุดส่งซ้ำและแจ้งผู้ดูแล | Apps Script ทั้งสองโปรเจกต์ต้องตั้ง `ALLOW_LEGACY_DIRECT=TRUE` |
| ส่งใบแล้วไม่แน่ใจว่าสำเร็จ | ตรวจแท็บ **ของฉัน** ก่อนส่งใหม่ | Request ID และใบซ้ำใน Notion |
| ไม่พบผู้อนุมัติ | ติดต่อผู้ดูแล | ชื่อ Staff, กลุ่มงาน และสายอนุมัติต้องตรงกัน |
| จำนวนวันไม่ตรง | ตรวจวันเริ่ม/สิ้นสุดและประเภทลา | Holidays และหน่วยวันทำการ/วันปฏิทิน |
| รหัสผู้ดูแลไม่ถูกต้อง | ตรวจว่าคัดลอกครบและยังไม่ถูกหมุน | `ADMIN_TOKEN` และ Apps Script Executions โดยห้ามพิมพ์ token ลง log |
| เชื่อมต่อระบบภายในไม่สำเร็จ | รอสักครู่แล้วลองหนึ่งครั้ง | Apps Script Executions, Notion/LINE status และ requestId |
| การแจ้งเตือนต้องตรวจสอบ | ไม่ส่งซ้ำเองหลายครั้ง | สาเหตุ LINE failure, retry count และ dead-letter status |

## 8. การแจ้งปัญหาอย่างปลอดภัย

ผู้แจ้งควรส่ง:

- วันเวลาโดยประมาณและหน้าที่ใช้งาน
- ข้อความผิดพลาดภาษาไทยหรือ error code
- Request ID ถ้าหน้าจอแสดง
- ประเภทอุปกรณ์/เบราว์เซอร์และขั้นตอนที่ทำก่อนเกิดปัญหา
- screenshot ที่ปิดชื่อ เหตุผลการลา token และข้อมูลส่วนบุคคลที่ไม่จำเป็นแล้ว

ห้ามส่ง password, admin token, LINE/Notion secret, shared secret, access token หรือ URL ที่มี credential ผู้ดูแลเทคนิคควรเชื่อมเหตุการณ์จาก requestId/timestamp ใน log แทนการขอ secret จากผู้ใช้

## 9. แนวทางสำหรับผู้รับช่วงพัฒนาต่อ

1. อ่าน [README.md](README.md) เพื่อระบุว่าแก้ส่วนใดและต้อง deploy ที่ใด
2. อ่าน [SETUP.md](SETUP.md) ก่อนเปลี่ยน schema ของ Sheet/Notion หรือชื่อ property ภาษาไทย
3. รักษา backward compatibility ของชื่อประเภทลาและข้อมูลเดิมระหว่าง migration
4. อย่ารวมการตัดสินสิทธิ์ทางกฎหมายไว้ใน logic อัตโนมัติโดยไม่มี version/effective date และการรับรองจาก HR
5. ระบบเลือก direct mode: browser ใช้ GET ไป Apps Script `/exec`; ห้ามบันทึก URL ที่มี token ลง screenshot, issue หรือ log และต้องคง `ALLOW_LEGACY_DIRECT=TRUE` อย่างชัดเจน
6. เพิ่ม deterministic test สำหรับ validation, authorization, idempotency และ failure path ทุกครั้งที่เปลี่ยน boundary สำคัญ
7. รันคำสั่งต่อไปนี้ก่อนส่งมอบ:

```bash
node scripts/check-syntax.js
node scripts/test-apps-script.js
cd gateway && npm test
```

8. อัปเดตคู่มือนี้, [SETUP.md](SETUP.md) และ runbook เมื่อพฤติกรรมของผู้ใช้หรือผู้ดูแลเปลี่ยน
